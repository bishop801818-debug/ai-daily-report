# 🐛 首页自动错误收集与记录机制

## 📋 系统概述

为首页 `index_v3.html` 添加了**自动错误收集与记录机制**，能够自动捕获和记录开发过程中出现的各种错误，帮助快速定位和解决问题。

---

## ✨ 核心功能

### 1. 自动错误捕获
- ✅ **JavaScript运行时错误**（语法错误、引用错误等）
- ✅ **Promise未捕获错误**（异步代码错误）
- ✅ **网络请求错误**（fetch/XHR请求失败、HTTP错误状态码）
- ✅ **资源加载错误**（JS/CSS/图片加载失败）
- ✅ **console.error调用**（自动拦截并记录）

### 2. 错误分类与记录
每条错误记录包含：
- 📝 **错误ID**：唯一标识符
- ⏰ **时间戳**：精确到毫秒
- 🏷️ **错误类型**：JS错误、网络错误、渲染错误等
- 📊 **错误分类**：自动判断错误类别
- 💬 **错误消息**：完整的错误描述
- 🔍 **堆栈信息**：详细的调用堆栈
- 💡 **解决建议**：自动匹配常见错误的解决方案
- 🌐 **环境信息**：浏览器UserAgent、当前URL

### 3. 错误查看界面
- 📊 **统计面板**：总错误数、分类统计、今日错误
- 🔍 **搜索过滤**：按错误消息搜索、按分类过滤
- 📖 **详细展示**：错误消息、解决建议、堆栈信息
- 📥 **导出功能**：导出为JSON或CSV格式
- 🗑️ **清空功能**：一键清空所有错误日志

---

## 🚀 使用方法

### 步骤1：启动首页
1. 启动Web服务器（端口8888）
2. 启动API代理服务器（端口5000）
3. 访问首页：http://localhost:8888/index_v3.html

**错误监控会自动启动**，在控制台可以看到：
```
[错误监控] 初始化中...
[错误监控] ✅ 初始化完成
[错误监控] 使用说明:
  - 查看日志: getErrorLogs()
  - 清空日志: clearErrorLogs()
  - 导出日志: exportErrorLogs("json") 或 exportErrorLogs("csv")
  - 手动记录: logCustomError("错误消息", "错误分类")
```

### 步骤2：查看错误日志

**方法1：使用错误查看器**（推荐）
1. 访问：http://localhost:8888/error-viewer.html
2. 查看统计面板和错误列表
3. 使用搜索和过滤功能定位错误
4. 点击"显示堆栈信息"查看详细堆栈
5. 使用导出功能下载错误日志

**方法2：使用浏览器控制台**
1. 按 `F12` 打开控制台
2. 输入 `getErrorLogs()` 查看所有错误
3. 输入 `clearErrorLogs()` 清空日志
4. 输入 `exportErrorLogs('json')` 导出为JSON

### 步骤3：分析错误趋势

定期查看错误日志，关注：
- 📈 **错误频率**：哪些错误反复出现？
- 🔄 **错误模式**：是否在特定操作后出现？
- 🐛 **错误分类**：哪类错误最多？
- 💡 **解决建议**：按照建议修复问题

---

## 💡 常见使用场景

### 场景1：开发过程中自动记录错误
**操作**：
1. 正常开发，修改首页代码
2. 刷新页面，错误会自动记录到localStorage
3. 定期访问 `error-viewer.html` 查看错误

**好处**：
- 不会遗漏任何错误
- 可以看到错误的完整上下文
- 方便回溯错误原因

### 场景2：修复错误后验证
**操作**：
1. 在 `error-viewer.html` 中查看错误详情
2. 按照解决建议修复代码
3. 刷新首页，检查错误是否不再出现
4. 清空已解决的错误日志

### 场景3：团队协作时分享错误
**操作**：
1. 导出错误日志为JSON或CSV
2. 分享给团队成员
3. 团队成员导入或直接查看错误详情

---

## 🔧 高级功能

### 1. 手动记录错误
在代码中手动记录错误：
```javascript
// 在可能出现错误的地方
try {
    // 你的代码
    someFunction();
} catch (e) {
    // 手动记录错误
    logCustomError(
        e.message,
        '数据解析错误',
        { function: 'someFunction', data: someData }
    );
    // 可选：重新抛出错误
    throw e;
}
```

### 2. 自定义错误解决建议
编辑 `error-monitor.js` 中的 `ERROR_SOLUTIONS` 对象：
```javascript
const ERROR_SOLUTIONS = {
    '你的错误信息关键词': '你的解决建议',
    'Failed to load resource': '检查文件路径是否正确，文件是否存在',
    // 添加更多...
};
```

### 3. 自动上报错误到服务器（可选）
如果需要集中管理错误日志，可以启用自动上报：
1. 在 `error-monitor.js` 中修改配置：
   ```javascript
   const CONFIG = {
       enableAutoReporting: true,  // 改为 true
       reportUrl: 'http://localhost:5000/api/error-report'  // 你的上报API地址
   };
   ```
2. 在 `proxy_qwen.py` 中添加错误上报API端点

---

## 📊 错误分类说明

| 错误分类 | 说明 | 常见原因 | 解决建议 |
|---------|------|---------|---------|
| JavaScript错误 | JS代码运行时错误 | 语法错误、引用错误、类型错误 | 检查控制台错误信息，定位到具体代码行 |
| Promise未捕获错误 | 异步代码中的未捕获错误 | 网络请求失败、数据处理错误 | 添加 `.catch()` 或 `try/catch` |
| 网络请求错误 | fetch/XHR请求失败 | 服务器未启动、API地址错误、网络断开 | 检查服务器状态，验证API地址 |
| 资源加载错误 | JS/CSS/图片加载失败 | 文件路径错误、文件不存在 | 检查资源路径，确认文件存在 |
| 图表渲染错误 | ECharts图表渲染失败 | 数据格式错误、图表配置错误 | 检查数据格式，验证图表配置 |
| 数据解析错误 | JSON/数据解析失败 | 数据格式错误、数据缺失 | 检查数据源，添加数据校验 |
| API调用错误 | API请求失败 | API密钥错误、API地址错误 | 检查API配置，验证API密钥 |

---

## 🎯 最佳实践

### 1. 定期查看错误日志
- 建议每天开发结束后查看一次错误日志
- 关注新增错误和反复出现的错误
- 优先解决影响功能的错误

### 2. 按照解决建议修复
- 错误监控系统会自动提供解决建议
- 按照建议逐步排查和修复
- 修复后验证错误是否不再出现

### 3. 保持错误日志整洁
- 定期清空已解决的错误日志
- 导出重要错误日志备份
- 避免日志过多影响性能（已限制最多500条）

### 4. 结合版本控制
- 修复错误后在git commit中注明修复的错误
- 使用git bisect定位引入错误的提交
- 在运维文档中记录常见错误和解决方案

---

## 🔍 故障排查

### 问题1：错误监控未启动
**症状**：控制台没有看到 `[错误监控] 初始化中...` 信息

**原因**：
1. `error-monitor.js` 未正确引入
2. JS代码有语法错误导致监控脚本未执行

**解决**：
1. 检查 `index_v3.html` 的 `<head>` 中是否有：
   ```html
   <script src="error-monitor.js?v=20260617"></script>
   ```
2. 打开浏览器控制台，查看是否有JS错误
3. 手动在控制台输入 `logError` 看是否报错

### 问题2：错误未记录到localStorage
**症状**：`error-viewer.html` 显示"暂无错误日志"

**原因**：
1. localStorage被禁用
2. 浏览器隐私模式
3. 错误监控未正确初始化

**解决**：
1. 检查浏览器是否禁用localStorage
2. 退出隐私模式
3. 在控制台输入 `localStorage.setItem('test', '1')` 测试

### 问题3：错误日志过多
**症状**：localStorage占用过大

**解决**：
1. 定期清空已解决的错误日志
2. 修改 `error-monitor.js` 中的 `maxLogs` 配置（默认500条）
3. 导出重要日志后清空

---

## 📝 技术实现细节

### 1. 错误捕获原理
- **window.onerror**：捕获全局JS错误
- **unhandledrejection**：捕获Promise未处理错误
- **重写console.error**：拦截所有console.error调用
- **重写fetch/XHR**：监控网络请求错误
- **资源加载事件**：监控资源加载失败

### 2. 数据存储方案
- 使用 `localStorage` 存储错误日志
- 无需后端支持，纯前端实现
- 数据持久化，关闭浏览器后仍然存在

### 3. 性能优化
- 限制最大日志数量（默认500条）
- 使用防抖/节流避免频繁写入
- 错误堆栈信息压缩存储

---

## 🚀 未来改进方向

1. **错误聚合**：相同错误自动聚合，统计出现次数
2. **错误趋势图表**：可视化错误趋势，识别问题模块
3. **自动修复建议**：基于历史修复记录，提供更精准的建议
4. **团队协作**：错误日志云端同步，团队成员共享
5. **AI辅助诊断**：使用AI分析错误原因，提供修复代码

---

## 📞 支持与反馈

如有问题或改进建议，请联系开发者或提交issue。

**文档版本**：v1.0  
**创建日期**：2026-06-17  
**更新日期**：2026-06-17
