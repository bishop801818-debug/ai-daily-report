# 🛡️ 智能存档脚本增强版 - 使用指南

**版本**: v2.0  
**更新日期**: 2026-06-26  
**作者**: AI Assistant

---

## 🎯 解决的问题

### 原始问题
**风险场景**：某个子页面被错误回滚到旧版本 → 智能存档检测到"变化" → 提交 → **用旧版本覆盖了新版本**

### 解决方案
本增强版通过以下机制彻底解决这个问题：

1. **版本号保护** - 自动检测版本回退并阻止提交
2. **自动备份** - 提交前自动备份被覆盖的文件
3. **滚动备份** - 只保留最近N个备份（默认3个），节省空间

---

## 📦 功能详解

### 1. 版本号保护机制

#### 原理
- 在每个HTML文件中嵌入版本号（格式：`YYYYMMDD_HHMM`）
- 存档时自动比较：**当前版本 vs Git中的版本**
- 如果当前版本 < Git版本 → **阻止提交**（避免用旧版本覆盖新版本）

#### 版本号格式
支持3种格式（自动识别）：
```html
<!-- 格式1：meta标签 -->
<meta name="html-version" content="20260626_1800">

<!-- 格式2：JS变量 -->
window.HTML_VERSION = '20260626_1800';

<!-- 格式3：JS变量（var） -->
var HTML_VERSION = '20260626_1800';
```

#### 示例输出
```
🔍 比较文件变化 + 版本回退检查...
   ⚠️ index_v3.html: ⚠️ 版本回退检测：当前20260625_1200 < Git中20260626_1800
      ⛔ 已阻止提交（版本回退风险）
```

---

### 2. 自动备份机制

#### 原理
- 在提交前，自动备份**即将被覆盖的HTML文件**
- 备份内容 = Git中的当前版本（即"正确版本"）
- 如果发生意外，可以从备份恢复

#### 备份位置
```
archive/backups/
├── 20260626_180105/  # 时间戳命名的备份目录
│   ├── index_v3.html
│   ├── electrolyte_data_v2.html
│   └── ...
├── 20260627_140000/
│   └── ...
```

#### 示例输出
```
📦 创建备份（滚动机制：保留最近3个）...
   ✅ 已备份 53 个文件到 archive/backups\20260626_180105
```

---

### 3. 滚动备份机制

#### 原理
- 只保留最近 **N个备份**（默认3个）
- 超出数量的旧备份自动删除
- 避免备份文件堆积占用空间

#### 配置方法
编辑 `smart_commit_config.json`：
```json
{
  "max_backups": 3  // 修改为 5 即可保留5个备份
}
```

#### 示例输出
```
📦 创建备份（滚动机制：保留最近3个）...
   📊 备份目录统计: 4 个（保留最近3个）
   🗑️  删除旧备份: 20260625_120000
   ✅ 已备份 53 个文件到 archive/backups\20260626_180105
```

---

## 🚀 使用方法

### 方法1：命令行（推荐）

```bash
cd "D:/trae/AI Daily report"
python smart_git_commit.py
```

**输出示例**：
```
============================================================
🔍 智能Git存档开始（版本保护 + 自动备份）
============================================================

📁 收集文件...
   找到 92 个文件需要检查
   过滤后 84 个文件（已排除gitignore文件）

🔍 比较文件变化 + 版本回退检查...
   📝 index_v3.html: 内容有变化
   ⏭️  policy_center_v4.html: 内容无变化
   ⚠️ old_page.html: ⚠️ 版本回退检测：当前20260101_0000 < Git中20260626_1800
      ⛔ 已阻止提交（版本回退风险）

============================================================
✅ 有 81 个文件需要提交

📦 创建备份（滚动机制：保留最近3个）...
   ✅ 已备份 53 个文件到 archive/backups\20260626_180105

💾 提交: 智能存档: 2026-06-26 81个文件有变化
🚀 推送到远程仓库...

============================================================
✅ 智能存档完成！提交了 81 个文件
📦 备份位置: archive/backups\20260626_180105
============================================================
```

---

### 方法2：批处理脚本（最简单）

**文件位置**: `D:\trae\AI Daily report\智能存档.bat`

**使用方法**: 双击运行即可

**注意**：批处理脚本内部调用的是 `smart_git_commit.py`，所以增强功能自动生效。

---

## 📝 为现有HTML文件添加版本号

### 问题
版本保护机制需要HTML文件包含版本号。如果文件没有版本号，会跳过版本检查。

### 解决方案：批量添加版本号

运行以下命令，为所有HTML文件自动添加版本号：

```bash
cd "D:/trae/AI Daily report"
python add_version_to_html.py
```

**功能**：
- 自动查找所有 `*.html` 文件
- 如果已有版本号，跳过
- 如果没有版本号，在 `</head>` 前添加 `<meta name="html-version">` 标签
- 所有文件使用统一的版本号（运行时生成）

**输出示例**：
```
============================================================
📝 批量添加版本号到HTML文件
============================================================

找到 84 个HTML文件
统一版本号: 20260626_1805

   ✅ index_v3.html: 已添加版本号 20260626_1805
   ⏭️  policy_center_v4.html: 已有版本号，跳过
   ...

============================================================
✅ 完成！
   成功添加: 53 个文件
   跳过（已有版本号）: 31 个文件
============================================================
```

---

## ⚙️ 配置说明

### 配置文件
`smart_commit_config.json`

### 配置项详解

```json
{
  "compare_rules": {
    "*.html": {
      "method": "hash",       // 比较方法：hash（SHA256）= size（文件大小）
      "quick_check": "size"   // 快速检查：先比大小，不同才计算hash
    },
    "data/*.json": {
      "method": "hash",
      "quick_check": "size",
      "json_fields": ["update_time"]  // 只比较特定字段（可选）
    }
  },
  "exclude_patterns": [
    "data/*_temp.json",       // 排除临时文件
    "data/*_bak.json",
    "*_temp.html"
  ],
  "debug_mode": false,        // 调试模式：显示详细比较信息
  "commit_message_template": "智能存档: {date} {count}个文件有变化",
  "max_backups": 3            // 滚动备份：保留最近3个备份
}
```

### 自定义配置

| 需求 | 修改方法 |
|------|----------|
| 保留5个备份 | 修改 `"max_backups": 5` |
| 只比较文件大小（更快） | 修改 `"method": "size"` |
| 排除更多文件 | 在 `"exclude_patterns"` 中添加规则 |
| 启用调试模式 | 修改 `"debug_mode": true` |

---

## 🔧 故障排查

### 问题1：版本回退误报

**现象**：文件被错误标记为"版本回退"

**可能原因**：
1. 文件没有版本号
2. 版本号格式不正确
3. 版本号没有更新

**解决方案**：
```bash
# 检查文件的版本号
grep -i "html-version\|HTML_VERSION" index_v3.html

# 手动更新版本号
# 在 </head> 前添加或修改
<meta name="html-version" content="20260626_1800">
```

---

### 问题2：备份目录过大

**现象**：`archive/backups/` 占用太多空间

**解决方案**：
1. 修改 `smart_commit_config.json`，减少 `max_backups`
2. 手动删除旧备份：
```bash
cd "D:/trae/AI Daily report/archive/backups"
rmdir /s /q 20260625_120000  # Windows
```

---

### 问题3：脚本运行缓慢

**现象**：检查84个文件需要很长时间

**原因**：SHA256哈希计算较慢

**解决方案**：
```json
{
  "compare_rules": {
    "*.html": {
      "method": "size",  // 改用文件大小比较（快但不准确）
      "quick_check": "size"
    }
  }
}
```

---

## 🎯 最佳实践

### 1. 首次使用
```bash
# 步骤1：为所有HTML文件添加版本号
python add_version_to_html.py

# 步骤2：提交版本号更新
python smart_git_commit.py

# 步骤3：从此以后，正常存档即可
python smart_git_commit.py
```

### 2. 日常使用
- **推荐**：双击 `智能存档.bat`（自动调用增强版脚本）
- **次选**：命令行运行 `python smart_git_commit.py`

### 3. 版本号管理
- **重要更新**：手动更新版本号（修改HTML时）
- **自动更新**：运行 `add_version_to_html.py` 批量更新

---

## 📊 效果对比

| 项目 | 原始版本 | 增强版本 |
|------|----------|----------|
| **版本回退检测** | ❌ 无 | ✅ 自动检测并阻止 |
| **自动备份** | ❌ 无 | ✅ 提交前自动备份 |
| **滚动备份** | ❌ 无 | ✅ 只保留最近3个 |
| **错误覆盖风险** | ⚠️ 高 | ✅ 低（有保护+可恢复） |
| **存档速度** | 快 | 稍慢（多了版本检查和备份） |

---

## 🚨 重要提醒

1. **首次使用必须添加版本号**：
   ```bash
   python add_version_to_html.py
   ```

2. **修改HTML文件时记得更新版本号**：
   - 在 `</head>` 前添加/修改：
     ```html
     <meta name="html-version" content="YYYYMMDD_HHMM">
     ```

3. **定期检查备份目录**：
   - 位置：`archive/backups/`
   - 如果空间不足，减少 `max_backups` 配置

4. **如果需要强制提交（覆盖版本保护）**：
   - 先手动更新版本号
   - 再运行 `smart_git_commit.py`

---

## 📞 技术支持

如果遇到问题，请提供以下信息：
1. 错误信息截图
2. `smart_git_commit.py` 的输出日志
3. 文件的版本号（如果有）

---

**文档版本**: v2.0  
**最后更新**: 2026-06-26  
**对应脚本版本**: smart_git_commit.py v2.0
