# 数据库 & 可视化看板 — 完整更新流程

> **⚠️ 元数据（每次更新时必须同步修改！）**
> - **最后更新**：2026-06-09
> - **版本**：v1.7
> - **维护人**：AI Assistant
> - **总览对应章节**：[运维总览.md - 二、所有页面清单](运维总览.md#二所有页面清单)
> 
> 涵盖 7 个行业数据库：磷酸铁锂 / 碳酸锂 / 电解液 / 锂电池 / 汽车 / 回收 / 三元材料

---

## 一、整体架构

```
Excel 源文件 (*.xlsx)
    │
    ▼  (手动/外部工具转换 — 项目内无自动化脚本)
    │
*_embedded_data.js  ←── 数据源头（唯一权威来源）
    │
    ├── *_data_v2.html   (数据库页面，动态加载 JS 渲染表格)
    │
    └── *_charts.html    (可视化看板，两种模式 ↓)
            │
            ├── 模式A: 动态加载 JS → 图表 (3个看板)
            └── 模式B: 硬编码 var 数组 → 图表 (4个看板)
                              │
                              └── _update_charts_data.py 自动更新 (仅2个)
```

---

## 二、7 个数据库总览

| # | 行业 | 数据库页面 | 嵌入式 JS | JS 变量名 | 看板 | 看板数据模式 | all_data.json |
|---|------|-----------|-----------|-----------|------|-------------|---------------|
| 1 | 磷酸铁锂 | `lfp_data_v2.html` | `embedded_data.js` | `EMBEDDED_DATA` | `lfp_charts.html` | **硬编码** (需手动/脚本更新) | 有 |
| 2 | 碳酸锂 | `carbonate_data_v2.html` | `carbonate_embedded_data.js` | `EMBEDDED_DATA` | `carbonate_charts.html` | **硬编码** (需手动/脚本更新) | 有 |
| 3 | 电解液 | `electrolyte_data_v2.html` | `electrolyte_embedded_data.js` | `ELECTROLYTE_DATA` | `electrolyte_charts.html` | **动态** (自动读取 JS) | 有 |
| 4 | 锂电池 | `lib_battery_data_v2.html` | `lib_battery_embedded_data.js` | `EMBEDDED_DATA` | `lib_battery_charts.html` | **动态** (自动读取 JS) | 无 |
| 5 | 汽车 | `automotive_data_v2.html` | `automotive_embedded_data.js` | `AUTOMOTIVE_DATA` | `automotive_charts.html` | **硬编码** (需手动更新) | 无 |
| 6 | 回收 | `recycling_data_v2.html` | `recycling_embedded_data.js` | `EMBEDDED_DATA` | `recycling_charts.html` | **硬编码** (需手动更新) | 无 |
| 7 | 三元材料 | `ternary_data_v2.html` | `ternary_embedded_data.js` | `TERNARY_DATA` | `ternary_charts.html` | **动态** (自动读取 JS) | 有 |

---

## 三、数据源头：Excel → embedded JS

### 3.1 现状

`*_embedded_data.js` 文件是**所有数据库和看板的唯一权威数据源**。项目内**没有** Python 脚本能直接从 Excel 生成这些 JS 文件。它们来自外部渠道（飞书多维表格导出、Excel 手动转换等）。

### 3.2 embedded JS 文件结构

所有 JS 文件结构一致：

```javascript
const EMBEDDED_DATA = {   // 变量名因数据库而异
  "update_time": "2026-05-28 17:28:32",
  "source": "飞书多维表格-磷酸铁锂产业链数据库（Excel追加更新）",
  "tables": [
    {
      "table_name": "LFP-行业整体产量",
      "data": [
        { "月份": "2026-04", "月度产量": 46980, "产能利用率": "80%" },
        ...
      ]
    },
    ...
  ]
}
```

### 3.3 各数据库 JS 变量名对照

| 文件 | 变量名 |
|------|--------|
| `embedded_data.js` (LFP) | `EMBEDDED_DATA` |
| `carbonate_embedded_data.js` | `EMBEDDED_DATA` |
| `electrolyte_embedded_data.js` | `ELECTROLYTE_DATA` |
| `lib_battery_embedded_data.js` | `EMBEDDED_DATA` |
| `automotive_embedded_data.js` | `AUTOMOTIVE_DATA` |
| `recycling_embedded_data.js` | `EMBEDDED_DATA` |
| `ternary_embedded_data.js` | `TERNARY_DATA` |

### 3.4 特殊注意

- **LFP** 的文件名是 `embedded_data.js`（无 `lfp_` 前缀），容易混淆
- `embedded/embedded_data.js` 是旧版本（2026-05-18），根目录的是新版（2026-05-28），**更新时以根目录为准**
- `lfp_all_data.js` 是另一个 JS 数据文件，变量名也是 `EMBEDDED_DATA`，与 `embedded_data.js` 内容不同

---

## 四、数据库页面更新流程

### 4.1 前提

数据库页面（`*_data_v2.html`）是**动态渲染**的——它们加载 `*_embedded_data.js`，用 JavaScript 遍历 `tables` 数组生成 HTML 表格。因此：

> **只要更新了 `*_embedded_data.js`，数据库页面就自动展示最新数据，无需修改 HTML。**

### 4.2 更新步骤

```
1. 获取最新 Excel 数据源
2. 更新对应的 *_embedded_data.js 文件
3. 同步到 embedded/ 目录
4. 验证: http://localhost:8888/{prefix}_data_v2.html
```

### 4.3 各页面加载方式细节

| 数据库页面 | 数据加载方式 | 回退机制 |
|-----------|-------------|---------|
| `lfp_data_v2.html` | `<script src="embedded_data.js">` → `EMBEDDED_DATA.tables` | `fetch('lfp_all_data.json')` |
| `carbonate_data_v2.html` | `<script src="carbonate_embedded_data.js">` → `EMBEDDED_DATA.tables` | `fetch('carbonate_all_data.json')` |
| `electrolyte_data_v2.html` | `<script src="electrolyte_embedded_data.js">` → `ELECTROLYTE_DATA.tables` | 无回退 |
| `lib_battery_data_v2.html` | `<script src="lib_battery_embedded_data.js">` → `EMBEDDED_DATA.tables` | `fetch('lib_battery_all_data.json')` (但 JSON 不存在) |
| `automotive_data_v2.html` | `<script src="automotive_embedded_data.js">` → `AUTOMOTIVE_DATA` | 内联 HTML 备用行 |
| `recycling_data_v2.html` | `<script src="recycling_embedded_data.js">` → `EMBEDDED_DATA.tables` | `fetch('recycling_all_data.json')` (但 JSON 不存在) |
| `ternary_data_v2.html` | `<script src="ternary_embedded_data.js">` → `TERNARY_DATA.tables` | `fetch('ternary_all_data.json')` |

---

## 五、可视化看板更新流程

### 5.1 两种数据模式

#### 模式 A：动态加载（3 个看板）

**看板**: `electrolyte_charts.html`, `lib_battery_charts.html`, `ternary_charts.html`

这些看板直接加载 `*_embedded_data.js`，通过 `getTable()` 等辅助函数动态提取数据渲染图表。

**更新方式**：更新 embedded JS → 看板自动跟随，**无需修改 HTML**。

```javascript
// 示例 (electrolyte_charts.html)
var data = ELECTROLYTE_DATA;
function getTable(name) {
    return data.tables.find(t => t.table_name === name)?.data || [];
}
var industryData = getTable('电解液-行业整体产量');
// 动态提取月份、产量等字段 → 渲染 ECharts
```

#### 模式 B：硬编码数组（4 个看板）

**看板**: `lfp_charts.html`, `carbonate_charts.html`, `automotive_charts.html`, `recycling_charts.html`

这些看板在 HTML 中直接写了 `var LFP_MONTHS = [...]` 这样的 JS 数组，不读取 embedded JS。

**更新方式**：必须修改 HTML 中的硬编码数组。

### 5.2 各看板硬编码变量清单

#### LFP (`lfp_charts.html`)

| 变量名 | 含义 | 数据来源表 |
|--------|------|-----------|
| `LFP_MONTHS` | 近12个月份 | `LFP-行业整体产量` |
| `LFP_OUTPUT` | LFP月产量(万吨) | `LFP-行业整体产量` |
| `FP_OUTPUT` | FP月产量(万吨) | `FP-行业整体产量` |
| `LFP_UTIL` | 产能利用率(%) | `LFP-行业整体产量` |
| `TOP10_NAMES` | 企业名(最新月TOP10) | `LFP-分企业产量` |
| `TOP10_OUTPUT` | 企业产量(万吨) | `LFP-分企业产量` |
| `EXP_VOL` | 月出口量(万吨) | `LFP-出口量` |
| `EXP_PRICE` | 出口均价(万元/吨) | `LFP-出口量` |
| 图表 x1 | 现货日度价格(近14天) | `LFP现货市场价` |
| 图表 x2 | 磷矿石月度均价(近10月) | `LFP现货市场价` (月均) |
| 图表 e2 | 贸易伙伴TOP10 | `LFP-出口贸易伙伴` |
| 图表 e3 | 客户采购量TOP10 | `LFP竞对销量（客户采购量）` |

#### Carbonate (`carbonate_charts.html`)

| 变量名 | 含义 | 数据来源表 |
|--------|------|-----------|
| `lcMonths` | 近12个月份 | `碳酸锂-行业总产量` |
| `lcOutput` | Li₂CO₃月产量(吨) | `碳酸锂-行业总产量` |
| `lcUtil` | 产能利用率(%) | `碳酸锂-行业总产量` |
| `lhOutput` | LiOH月产量(吨) | `氢氧化锂-行业总产量` |
| `lcExpVol` | 出口量(吨) | `碳酸锂-出口量` |
| `lcExpPrice` | 出口均价(万元/吨) | `碳酸锂-出口量` |
| `lhExpVol` | LiOH出口量(吨) | `氢氧化锂-出口量` |
| `lhExpPrice` | LiOH出口均价(万元/吨) | `氢氧化锂-出口量` |
| `lcCompNames` | 企业名(最新月TOP10) | `碳酸锂-分企业产量` |
| `lcCompOutput` | 企业产量(吨) | `碳酸锂-分企业产量` |
| `lhCompNames` | LiOH企业名(TOP6) | `氢氧化锂—分企业产量` |
| `lhCompOutput` | LiOH企业产量(吨) | `氢氧化锂—分企业产量` |
| `lcDailyDates/AVG` | 日度价格(近15天) | `碳酸锂—价格` |
| `lcPriceMonths/AVG` | 月度均价(近8月) | `碳酸锂—价格` |
| `lspDates/AVG` | 锂辉石日度价(近10天) | `锂辉石精矿-价格` |
| `lymDates/AVG` | 锂云母日度价(近10天) | `锂云母—价格` |
| `lhDailyDates/AVG` | LiOH日度价(近6天) | `氢氧化锂-价格` |

#### Automotive (`automotive_charts.html`)

| 变量名 | 含义 | 数据来源表 |
|--------|------|-----------|
| `CHINA_MONTHS/SALES` | 中国整体销量(近14月) | `汽车-中国整体销量` |
| `NEV_MONTHS/SALES` | 新能源乘用车销量(近14月) | `中国新能源-乘用车-销量（分动力）` |
| `RETAIL_MONTHS/SALES` | 零售销量 | 同上 |
| `TYPE_MONTHS/BEV_HIST/PHEV_HIST` | 分动力类型 | 同上 |
| `BRAND_NAMES/SALES` | 车企排名(最新月TOP10) | `中国新能源-乘用车-车企排名` |
| `CV_NAMES/SALES` | 商用车车企排名 | `中国新能源-商用车-车企排名` |
| `EXP_MONTHS/SALES` | 出口销量(近14月) | `中国新能源-乘用车出口销量` |
| `EXP_OEM_NAMES/SALES` | 出口分车企(TOP10) | `中国新能源-乘用车出口分车企` |
| `ZC_NAMES/VOL/PCT` | 重卡保险量(TOP10) | `中国新能源-重卡保险量` |
| `QC_NAMES/VOL/PCT` | 轻卡保险量(TOP10) | `中国新能源-轻卡保险量` |
| `STAT_*` | 统计卡片数据 | 汇总计算 |

#### Recycling (`recycling_charts.html`)

| 变量名 | 含义 | 数据来源表 |
|--------|------|-----------|
| `T0_MONTHS/TOTALS/YOY/MOM` | 回收总量(近14月) | 回收行业数据表 |
| `T1_TOTALS` | 三元回收量 | 同上 |
| `T2_TOTALS` | 磷酸铁锂回收量 | 同上 |
| 其他硬编码数组 | TOP10企业、金属价格等 | 对应数据表 |

### 5.3 更新方式

#### 自动化（仅 LFP + Carbonate）

```bash
cd "D:\trae\AI Daily report"
python _update_charts_data.py
```

该脚本：
1. 读取 `embedded_data.js` / `carbonate_embedded_data.js`
2. 解析 JSON，提取各表最新数据
3. 用正则替换 HTML 中的硬编码 `var` 数组
4. 同步写入 `embedded/` 目录

**脚本覆盖的更新项**：
- `update_lfp()`: LFP_MONTHS, LFP_OUTPUT, FP_OUTPUT, LFP_UTIL, TOP10_NAMES, TOP10_OUTPUT, EXP_VOL, EXP_PRICE, 图表 x1, x2, e2, e3
- `update_carbonate()`: lcMonths, lcOutput, lcUtil, lhOutput, lcExpVol, lcExpPrice, lhExpVol, lhExpPrice, lcCompNames, lcCompOutput, lhCompNames, lhCompOutput, lcDailyDates, lcDailyAvg, lcPriceMonths, lcPriceAvg, lspDates, lspAvg, lymDates, lymAvg, lhDailyDates, lhDailyAvg

#### 手动（其他 5 个看板）

对于 Automotive、Recycling（硬编码模式）和 Electrolyte、LIB、Ternary（动态模式）：

- **动态模式看板**：更新 embedded JS 即可，无需改 HTML
- **硬编码模式看板**：需手动编辑 HTML 中的 `var` 数组，或手工运行数据提取逻辑

---

## 六、文件部署结构

```
D:\trae\AI Daily report\
├── {prefix}_data_v2.html          ← 数据库页面 (7个)
├── {prefix}_charts.html           ← 可视化看板 (7个)
├── {prefix}_embedded_data.js      ← 嵌入式数据 (7个)
├── {prefix}_all_data.json         ← JSON回退 (仅4个有)
│
├── embedded/                      ← 8089 嵌入版部署目录
│   ├── {prefix}_data_v2.html      ← 数据库页面副本
│   ├── {prefix}_charts.html       ← 看板副本 (仅5个)
│   ├── {prefix}_embedded_data.js  ← 数据副本 (7个)
│   └── ...（首页、Hub等中心页面）
│
├── 磷酸铁锂/                      ← 行业存档目录
├── 碳酸锂/
├── 电解液/
├── 锂电池/
├── 汽车行业/
├── 回收行业/
├── 三元材料/
│   └── (各存 charts.html + data_v2.html + embedded_data.js)
│
└── db_archives/
    ├── 8888/                      ← 8888端存档 (按行业)
    └── embedded/                  ← 嵌入版存档 (按行业)
```

---

## 七、完整更新 SOP（标准操作流程）

### 场景 1：更新某数据库的 embedded JS（数据内容更新）

```
1. 获取最新 Excel 数据源
2. 更新根目录 {prefix}_embedded_data.js
3. cp 到 embedded/{prefix}_embedded_data.js
4. 验证: http://localhost:8888/{prefix}_data_v2.html
5. 如果是动态模式看板 → 看板自动更新，完成
6. 如果是硬编码模式看板 → 继续场景 2
```

### 场景 2：更新硬编码看板数据

```
1. 确保 embedded JS 已是最新
2. LFP + Carbonate: 运行 python _update_charts_data.py
3. Automotive + Recycling: 手动编辑 {prefix}_charts.html 中的 var 数组
4. cp 看板 HTML 到 embedded/
5. 验证: http://localhost:8888/{prefix}_charts.html
```

### 场景 3：存档到行业文件夹

```
1. cp {prefix}_data_v2.html → {行业名}/
2. cp {prefix}_charts.html → {行业名}/
3. cp {prefix}_embedded_data.js → {行业名}/  (如存在)
```

### 场景 4：同步到 8089

```
cd D:\trae\AI Daily report
python embedded\gen_embedded_all.py
```

---

## 八、政策中心 (policy_center_v4.html) 更新逻辑

### 8.1 数据流概览

```
每日 MD 文件 (9 BU)
    │
    ▼  D:\buddy\md_to_json.py 解析
    │
reports/YYYY-MM-DD.json  (每日早报)
    │
    ├── build_policies_from_reports() → reports/policies.json (累积635+条)
    │
    ▼  embedded/gen_embedded_all.py 嵌入
    │
policy_center_v4.html  ← var EMBEDDED_POLICIES = [...] (inline, ~7.3MB)
    │
    └── fallback: fetch('policies_source/policies_clean.json')
```

### 8.2 数据来源

| 优先级 | 数据来源 | 说明 |
|--------|---------|------|
| **第一优先** | `var EMBEDDED_POLICIES = [...]` | 内嵌在 HTML 第 388 行，混合了每日政策 + 静态 12 条核心政策 |
| **回退** | `policies_source/policies_clean.json` | 124KB，当 EMBEDDED_POLICIES 不存在时 fetch |
| **嵌入版** | `embedded/policy_center_embedded_data.js` | `window.__EMBEDDED__` 对象，含 report + policies |

### 8.3 政策数据来源分类

1. **每日政策** (`_isDaily: true`): 从 9 BU 早报的 `dim === "政策"` 板块提取，`md_to_json.py` 的 `build_policies_from_reports()` 自动累积
2. **静态核心政策** (`_isDaily: false`, id: "1"-"12"): 由 `reports/gen_policies.py` 硬编码生成，涵盖中央/地方/海外重大政策

### 8.4 更新方式

```
1. 每日早报生成 (D:\buddy\md_to_json.py)
   └── 自动调用 build_policies_from_reports()
   └── 新政策追加到 reports/policies.json（按 date+title[:50] 去重）

2. 生成嵌入版
   cd D:\trae\AI Daily report
   python embedded\gen_embedded_all.py
   └── 将 policies.json 注入 policy_center_v4.html 的 EMBEDDED_POLICIES
   └── 同时更新 embedded/policy_center_embedded_data.js

3. 验证
   http://localhost:8888/policy_center_v4.html
```

### 8.5 页面加载逻辑

```javascript
// 优先使用内嵌数据
async function loadPoliciesJson() {
    const data = window.EMBEDDED_POLICIES;  // inline 7MB 数组
    return Array.isArray(data) ? data : [];
}

// 嵌入版有 fetch override (window.__EMBEDDED__)
// 拦截 fetch('reports/*.json') → 返回内嵌 report
// 拦截 fetch('policies.json') → 返回 window.__EMBEDDED__.policies
```

### 8.6 政策类型分类规则（中央/地方/海外）

分类逻辑位于 `D:\buddy\md_to_json.py` 的 `build_policies_from_reports()` 函数中，**纯关键词匹配，无人工审核环节**。

#### 分类流程

```
默认 → "central" (中央)
  ↓ 检查正文是否包含地方关键词 → 匹配 → "local" (地方)
  ↓ 不匹配 → 检查正文是否包含海外关键词 → 匹配 → "overseas" (海外)
  ↓ 不匹配 → 保持 "central"
```

优先级：中央 → 地方 → 海外。一旦匹配到"地方"，就不会再判为"海外"。

#### 关键词清单 (`TYPE_KEYWORDS`)

| 类型 | 关键词 |
|------|--------|
| **中央** (`central`) | 国务院、工信部、发改委、能源局、商务部、财政部、生态环境部、国家、四部门、六部门、八部门、锂电池、动力电池、储能电池 |
| **地方** (`local`) | 省、市、区、县、地方、江苏、浙江、广东、山东、四川、北京、上海、深圳、广州 |
| **海外** (`overseas`) | 欧盟、美国、欧洲、德国、日本、韩国、IRA、EU |

#### 关键实现细节

- 匹配对象是政策**正文内容**（`item.get('content', '')`），不是标题
- 判断方式：`any(kw in content for kw in keywords)`
- `_isDaily: true` 的政策来自 9 BU 早报 `dim === "政策"` 板块
- `_isDaily: false` 的 12 条静态核心政策由 `reports/gen_policies.py` 硬编码生成，`type` 字段手动指定
- **无独立的分类运维文档**，此分类规则此前仅存在于代码中

---

## 九、行业新闻 (industry_news_embedded.html) 更新逻辑

### 9.1 数据流概览

```
每日 MD 文件 (9 BU)
    │
    ▼  D:\buddy\md_to_json.py 解析
    │
reports/YYYY-MM-DD.json  (每日早报，60天回溯)
    │
    ├── 方式 A: gen_embedded_all.py → 嵌入到 HTML 的 EMBEDDED_NEWS 变量
    │
    ├── 方式 B: _filter_industry_news.py → industry_news_source/industry_news_clean.json (1.6MB, 2476条)
    │
    ▼
industry_news_embedded.html  ← const EMBEDDED_NEWS = [...] (inline, ~8.7MB)
    │
    └── fallback: loadAllReports() → fetch reports/*.json 动态聚合
```

### 9.2 数据来源

| 优先级 | 数据来源 | 说明 |
|--------|---------|------|
| **第一优先** | `const EMBEDDED_NEWS = [...]` | 内嵌在 HTML 第 256 行，预清洗的新闻数组 |
| **回退 1** | `industry_news_source/industry_news_clean.json` | 1.6MB，2476 条清洗后新闻 |
| **回退 2** | `loadAllReports()` | 动态 fetch reports/ 下所有日期 JSON，60 天回溯 |

### 9.3 数据加载机制

```javascript
// 方式 1: 直接用内嵌清洗数据
const CLEAN_NEWS_URL = 'industry_news_source/industry_news_clean.json';
const items = typeof EMBEDDED_NEWS !== 'undefined' ? EMBEDDED_NEWS : [];

// 方式 2: 动态聚合 (60天范围)
async function loadAllReports() {
    // 1. fetch reports/index.json → 获取可用日期列表
    // 2. 并行加载所有 reports/YYYY-MM-DD.json
    // 3. 按 deptId 合并各部门 sections
    // 4. normalizeDim() 归一化维度 (政策→policy, 市场→market, etc.)
    // 5. 按 title[:60] + deptId 去重
    // 6. 返回 { data: mergedDepts, dateRange, loadedDates }
}
```

### 9.4 维度分类

> **详细分类规则（DIM_MAP、模糊关键词、两级匹配逻辑）见 [01-早报运维详细文档 - 8.4 维度分类规范](01-早报运维详细文档.md#84-维度分类规范新闻归类逻辑)。**

简要映射：

| 原始维度 | 归一化 key | TAB 标签 |
|---------|-----------|---------|
| 政策/行业 | `policy` | 宏观新闻 |
| 市场/价格 | `market` | 行业新闻 |
| 竞品/企业动态/项目/客户 | `competitor` | 企业动态 |
| 前沿/技术/产品 | `frontier` | 技术产品 |

### 9.5 更新方式

```
1. 每日早报生成 (D:\buddy\md_to_json.py)
   └── 生成 reports/YYYY-MM-DD.json

2. 清洗新闻 (如需更新 industry_news_clean.json)
   python _filter_industry_news.py  (**此脚本尚待补充**)
   └── 从 reports/*.json 提取、去重、清洗 → industry_news_clean.json

3. 生成嵌入版
   cd D:\trae\AI Daily report
   python embedded\gen_embedded_all.py
   └── 将 clean JSON 注入 industry_news_embedded.html 的 EMBEDDED_NEWS

4. 验证
   http://localhost:8888/industry_news.html
   http://localhost:8888/industry_news_embedded.html
```

### 9.6 两个版本说明

| 文件 | 大小 | 用途 |
|------|------|------|
| `industry_news.html` | 根目录动态版 | fetch reports/*.json 动态聚合 |
| `industry_news_embedded.html` | 8.7MB 嵌入版 | 数据全内嵌，单文件可离线运行（8089用） |

### 9.7 新闻维度分类规则

> **详细内容已移至 [01-早报运维详细文档 - 8.4 维度分类规范](01-早报运维详细文档.md#84-维度分类规范新闻归类逻辑)**，包含完整的 DIM_MAP、模糊关键词清单、匹配顺序和关键实现细节。此处不再重复。

---

## 十、跨页面数据流汇总

```
                     D:\buddy\md_to_json.py
                            │
              MD文件 → reports/YYYY-MM-DD.json
                            │
          ┌─────────────────┼──────────────────┐
          │                 │                  │
    policies.json    industry_news_     gen_embedded_all.py
    (build_policies  clean.json        (嵌入版一次性生成)
     _from_reports)  (清洗去重)
          │                 │                  │
          ▼                 ▼                  ▼
   policy_center_v4   industry_news_    所有 *_embedded.html
   (EMBEDDED_         embedded.html     和 embedded/*.js
    POLICIES)         (EMBEDDED_NEWS)
```

---

## 十一、已知问题 & 待改进

| 问题 | 影响 | 优先级 |
|------|------|--------|
| `_update_charts_data.py` 仅覆盖 LFP + Carbonate | Automotive、Recycling 看板缺乏自动更新 | 高 |
| LIB、Automotive、Recycling 缺少 `all_data.json` | 如果 JS 加载失败，数据库页面无回退数据 | 中 |
| LFP 使用无前缀文件名 `embedded_data.js` | 易与 `embedded/embedded_data.js` 混淆 | 中 |
| `embedded/embedded_data.js` 与根目录不同步 | 8089 可能展示旧数据 | 中 |
| JS 变量名不统一 (EMBEDDED_DATA vs ELECTROLYTE_DATA vs TERNARY_DATA) | 维护时需要记忆不同变量名 | 低 |
| 无 Excel → embedded JS 的自动化生成脚本 | 数据更新依赖外部工具/手动操作 | 低 |

---

## 十二、关联文件

| 文件 | 作用 |
|------|------|
| `_update_charts_data.py` | LFP + Carbonate 看板数据自动更新脚本 |
| `_update_wti_ur_data.py` | WTI 原油 / 尿素期货数据抓取 |
| `fetch_lithium_ore_price.py` | 锂矿价格抓取 |
| `update_lc_history.py` | 碳酸锂历史数据更新 |
| `CLAUDE.md` | 项目级说明（部分过时） |
| `embedded/gen_embedded_all.py` | 生成 8089 嵌入版（注入 EMBEDDED_POLICIES/EMBEDDED_NEWS） |
| `D:\buddy\md_to_json.py` | MD→JSON 核心转换 + build_policies_from_reports() |
| `reports/gen_policies.py` | 生成 12 条静态核心政策 → policies_source/policies.json |
| `industry_news_source/industry_news_clean.json` | 行业新闻清洗后数据 (2476条) |
| `policies_source/policies_clean.json` | 政策清洗后数据 |
| `embedded/policy_center_embedded_data.js` | 8089 嵌入版政策数据 (window.__EMBEDDED__) |

---

## 十三、关于 CLAUDE.md

> **注意**：项目根目录的 `CLAUDE.md` 部分内容已过时，以下引用需注意：
> - `_backup_db_pages.py`、`_db_source_config.py` — 文件不存在
> - "6 个数据库"应为 **7 个**（三元材料已添加）
>
> 详细准确信息以本文档为准。CLAUDE.md 的过时引用待后续统一清理。

---

## 十四、锂辉石/锂云母价格数据更新流程

> **⚠️ 本节覆盖 `index_v3.html`（信息中台首页）中的锂辉石、锂云母图表数据更新。**
> 数据文件：`data/lithium_ore_price_history.json`（锂辉石）、`data/lepidolite_price_history.json`（锂云母）
> 抓取脚本：`fetch_lithium_ore_price.py`（锂辉石）、更新脚本：`update_lithium_lepidolite_history.py`（锂辉石+锂云母）

### 14.1 数据文件结构

#### 锂辉石 `data/lithium_ore_price_history.json`

```json
{
  "update_time": "2026-06-08 08:24:00",
  "unit": "美元/吨",
  "history": [
    {
      "date": "2026-06-05",
      "grade": "5%",
      "origin": "澳洲",
      "min_price": 2310,
      "max_price": 2350,
      "avg_price": 2330,
      "unit": "美元/吨"
    }
  ]
}
```

**关键字段说明**：
- `grade`：品位，格式经历多次变更（详见14.2节数据源变更历史）
- `origin`：产地，格式经历多次变更（详见14.2节数据源变更历史）
- `unit`：**必须为 `"美元/吨"`**（锂辉石价格是美元计价，不是人民币）

#### 锂云母 `data/lepidolite_price_history.json`

```json
{
  "update_time": "2026-05-31 21:24:00",
  "unit": "元/吨",
  "history": [
    {
      "date": "2026-05-24",
      "grade": "锂云母精矿2.5%",
      "origin": "",
      "min_price": 6700,
      "max_price": 6700,
      "avg_price": 6700,
      "unit": "元/吨"
    }
  ]
}
```

**关键字段说明**：
- `grade`：品种，格式为 `"锂云母精矿2.5%"`（只保留2.5%品种，其他品种已清理）
- `unit`：**必须为 `"元/吨"`**（锂云母价格是人民币计价）

### 14.2 数据源变更历史

#### 锂辉石数据源变更（2026-05-24 和 2026-06-05）

| 时期 | `grade` | `origin` | 说明 |
|------|---------|----------|------|
| 2026-05-20及以前 | `"6%"` | `"澳洲"` | 旧格式，Mysteel原数据 |
| 2026-05-24起 | `"5.0-5.5%"` | `"澳大利亚"` | 新格式，Mysteel变更后数据 |
| 2026-06-05起 | `"5%"` | `"澳洲"` | **再次变更**，格式又变回类似旧格式 |

**影响**：`index_v3.html` 中锂辉石图表过滤条件需要随数据源变更而更新：
- 2026-05-24前：`grade === '6%' && origin === '澳洲'`
- 2026-05-24至2026-06-04：`grade === '5.0-5.5%' && origin === '澳大利亚'`
- 2026-06-05起：`grade === '5%' && origin === '澳洲'`

**⚠️ 重要**：每次Mysteel数据源格式变更，都必须同步更新 `index_v3.html` 中的过滤条件，否则图表会显示空白或旧数据。

#### 锂云母数据源说明

锂云母数据只保留 `"锂云母精矿2.5%"` 品种。其他品种（1.5%、1.8%、2.0%、2.2%）已清理，不保留。

### 14.3 自动化任务配置

| 任务ID | 任务名称 | 状态 | 执行时间 | 说明 |
|---------|----------|------|----------|------|
| `1779788037620` | 锂辉石锂云母价格数据自动更新 | **ACTIVE** | 每天17:00 | 执行 `fetch_lithium_ore_price.py` + `update_lithium_lepidolite_history.py`，更新数据文件并git push |
| `1779289771621` | 锂辉石+锂云母价格每日抓取 | **PAUSED** | — | 已禁用，冗余任务，不要启用 |

**注意**：只有 `1779788037620` 是活跃任务。`1779289771621` 已禁用，启用会导致重复抓取。

### 14.4 手动更新步骤

当需要手动更新锂辉石/锂云母数据时：

```bash
cd "D:\trae\AI Daily report"

# 1. 抓取最新数据
python fetch_lithium_ore_price.py

# 2. 更新历史数据文件（锂辉石+锂云母）
python update_lithium_lepidolite_history.py

# 3. 验证数据文件
python -c "import json; f=open('data/lithium_ore_price_history.json'); d=json.load(f); print('锂辉石最新:', d['history'][-1])"
python -c "import json; f=open('data/lepidolite_price_history.json'); d=json.load(f); print('锂云母最新:', d['history'][-1])"

# 4. 提交并推送
git add data/lithium_ore_price_history.json data/lepidolite_price_history.json fetch_lithium_ore_price.py update_lithium_lepidolite_history.py
git commit -m "update: 锂辉石/锂云母价格数据更新"
git push origin main
```

### 14.5 脚本说明

#### `fetch_lithium_ore_price.py`（锂辉石数据抓取）

- **数据源**：Mysteel网站（网页抓取）
- **输出**：`data/history/lithium_ore_price_YYYYMMDD.json`（每日抓取存档）
- **关键逻辑**：
  - 抓取锂辉石（**5%澳洲**）和锂云母（2.5%）数据
  - **注意**：Mysteel数据源格式会变更，脚本中的过滤条件需要随格式变更而更新（详见14.2节）
  - 单位：锂辉石 `"美元/吨"`，锂云母 `"元/吨"`
  - 如果抓取失败，使用 `data/history/` 目录下最新文件作为回退

#### `update_lithium_lepidolite_history.py`（历史数据更新）

- **输入**：`data/history/lithium_ore_price_YYYYMMDD.json`（最新抓取数据）
- **输出**：`data/lithium_ore_price_history.json`（锂辉石历史）、`data/lepidolite_price_history.json`（锂云母历史）
- **关键逻辑**：
  - `update_spodumene_history()`：只保留 `grade == '5%' && origin == '澳洲'` 的记录（**2026-06-05起格式**，历史格式详见14.2节）
  - `update_lepidolite_history()`：只保留 `grade` 包含 `'2.5%'` 的记录
  - 自动检测并统一单位（`normalize_price()` 函数）

### 14.6 常见错误与修复

| 错误现象 | 原因 | 解决方案 |
|---------|------|---------|
| 锂辉石图表数据停留在旧日期 | 数据源格式变更，过滤条件未更新 | 检查 `grade`/`origin` 是否匹配最新数据格式（详见14.2节） |
| 锂辉石图表显示空白 | Mysteel数据源格式变更，`index_v3.html` 过滤条件未同步更新 | 1. 检查最新数据格式（运行`python -c "import json; f=open('data/lithium_ore_price_history.json'); d=json.load(f); print(d['history'][-1])"`）<br>2. 更新 `index_v3.html` 中的过滤条件 |
| 锂云母图表显示多个品种锯齿状 | 数据文件包含多个品种未过滤 | 清理历史数据，只保留2.5%品种 |
| 锂辉石/锂云母图表显示 `"元/吨"` 但价格数量级错误 | 单位标错（锂辉石应是美元/吨） | 修正数据文件 `unit` 字段，锂辉石为 `"美元/吨"` |
| 自动化任务执行失败 | 脚本文件丢失或未提交git | 从git恢复或重建脚本，确保提交到git |

### 14.7 验证方法

更新后，在浏览器访问 `http://localhost:8888/index_v3.html`，检查：

1. **锂辉石图表**：最新价显示 `"2330 美元/吨"`（或最新价格），日期为最近交易日
2. **锂云母图表**：最新价显示 `"6700 元/吨"`（或最新价格），日期为最近交易日
3. **图表平滑**：没有出现同一天多个数据点的锯齿状波动（说明品种过滤正确）

---

## 十五、磷矿石价格数据更新流程

> **⚠️ 本节覆盖 `index_v3.html`（信息中台首页）中的磷矿石图表数据更新。**
> 数据文件：`data/phosphate_rock_price.json`（磷矿石现货价格）、`reports/market_cumulative_metals.json`（有色板块累计涨跌幅）、`reports/lfp_all_data.json`（LFP全产业链数据）
> 相关脚本：`add_phosphate_to_metals.py`（更新有色板块甘特图）、`fix_lfp_all_data_phosphate.py`（修复LFP数据）

### 15.1 数据文件结构

#### 磷矿石现货价格 `data/phosphate_rock_price.json`

```json
{
  "update_time": "2026-06-04 18:00:00",
  "unit": "元/吨",
  "data": [
    {
      "date": "2026-01-04",
      "price": 1000,
      "spec": "磷矿石30%品位",
      "source": "金投价格网"
    }
  ]
}
```

**关键字段说明**：
- `spec`：规格，固定为 `"磷矿石30%品位"`
- `unit`：**必须为 `"元/吨"`**（磷矿石价格是人民币计价）
- `price`：现货价格（整数，单位元/吨）

#### 有色板块累计涨跌幅 `reports/market_cumulative_metals.json`

```json
{
  "meta": {
    "update_time": "2026-06-08 13:00:00",
    "generator_version": "20260608_v2",
    "data_source": "东方财富"
  },
  "products": [
    {
      "name": "磷矿石",
      "unit": "元/吨",
      "start_date": "2026-01-04",
      "end_date": "2026-06-04",
      "start_price": 1000,
      "end_price": 1030,
      "change": 30.0,
      "change_pct": 3.0,
      "direction": "up",
      "data_points": 103,
      "price_series": [...]
    }
  ]
}
```

**关键字段说明**：
- `name`：必须为 `"磷矿石"`（与 `showProductChart()` 中的 `svgMap` 关键词匹配）
- `change_pct`：2026年累计涨跌幅（%）
- `price_series`：月度价格序列，用于甘特图点击后展示折线图

#### LFP全产业链数据 `reports/lfp_all_data.json`

```json
{
  "tables": [
    {
      "table_name": "磷酸盐价格数据",
      "fields": ["规格", "日期", "今日均价（万元/吨）"],
      "data": [
        {
          "规格": "磷矿石30%品位",
          "日期": "2026-01-04",
          "今日均价（万元/吨）": 0.1
        }
      ]
    }
  ]
}
```

**关键字段说明**：
- `table_name`：必须为 `"磷酸盐价格数据"`（`index_v3.html` 第12329行以此字符串匹配）
- `今日均价（万元/吨）`：价格单位**万元/吨**（不是元/吨），用于JavaScript计算月环比

### 15.2 展示位置

磷矿石数据在首页展示在两个位置：

| 位置 | 说明 | 数据来源 |
|------|------|---------|
| **有色板块甘特图** | "有色板块2026年累计涨跌幅"甘特图，磷矿石行 | `reports/market_cumulative_metals.json` |
| **LFP产业链全景图** | "磷酸铁锂产业链全景图"中的磷矿石卡片 | `reports/lfp_all_data.json` → `chainHistoryData['磷矿石']` |

**甘特图点击交互**：
- 点击甘特图"磷矿石"行 → 下方展开折线图
- 折线图数据来源：`data/phosphate_rock_price.json`（2026-01-01至今）
- 渲染函数：`renderPhosphateChart()`（`index_v3.html` 第16740行）

### 15.3 数据源

| 数据文件 | 数据源 | 更新频率 | 说明 |
|---------|---------|----------|------|
| `data/phosphate_rock_price.json` | 金投价格网/行业数据库 | 每日 | 磷矿石现货价格（30%品位） |
| `reports/market_cumulative_metals.json` | 基于 `phosphate_rock_price.json` 计算 | 每次更新磷矿石后 | 有色板块累计涨跌幅 |
| `reports/lfp_all_data.json` | 基于 `phosphate_rock_price.json` 转换 | 每次更新磷矿石后 | LFP产业链全景图数据源 |

### 15.4 自动化任务配置

| 任务ID | 任务名称 | 状态 | 执行时间 | 说明 |
|---------|----------|------|----------|------|
| `automation-1779169063282` | 碳酸锂+磷矿石现货价格自动抓取 | **活跃** | 每日（具体时间待查） | 生成 `phosphate_rock_price.json` 和 `carbonate_spot_price_merged.json` |

**注意**：磷矿石价格数据由自动化任务 `automation-1779169063282` 自动更新。如需手动更新，参考15.5节。

### 15.5 手动更新步骤

当需要手动更新磷矿石数据时：

```bash
cd "D:\trae\AI Daily report"

# 1. 更新磷矿石现货价格数据
#    （如果 data/phosphate_rock_price.json 不是最新的，需要先更新它）
#    数据来源：金投价格网/行业数据库
#    手动或脚本更新 data/phosphate_rock_price.json

# 2. 更新有色板块甘特图数据（market_cumulative_metals.json）
python "D:\buddy\add_phosphate_to_metals.py"

# 3. 修复 LFP全产业链数据（lfp_all_data.json 中的磷酸盐价格数据表）
python "D:\buddy\fix_lfp_all_data_phosphate.py"

# 4. 验证数据文件
python -c "import json; f=open('data/phosphate_rock_price.json'); d=json.load(f); print('磷矿石最新:', d['data'][-1])"
python -c "import json; f=open('reports/market_cumulative_metals.json'); d=json.load(f); p=[x for x in d['products'] if x['name']=='磷矿石'][0]; print('磷矿石累计涨跌幅:', p['change_pct'], '%')"

# 5. 提交并推送（如果需要）
git add data/phosphate_rock_price.json reports/market_cumulative_metals.json reports/lfp_all_data.json
git commit -m "update: 磷矿石价格数据更新"
git push origin main
```

### 15.6 脚本说明

#### `add_phosphate_to_metals.py`（更新有色板块甘特图）

- **输入**：`data/phosphate_rock_price.json`（磷矿石现货价格）
- **输出**：`reports/market_cumulative_metals.json`（有色板块累计涨跌幅，添加磷矿石）
- **关键逻辑**：
  - 筛选2026年数据（`date.startswith('2026-')`）
  - 计算累计涨跌幅：`(end_price - start_price) / start_price * 100`
  - 如果磷矿石已存在，更新；否则添加

#### `fix_lfp_all_data_phosphate.py`（修复LFP数据）

- **输入**：`data/phosphate_rock_price.json`（磷矿石现货价格）
- **输出**：`reports/lfp_all_data.json`（修复"磷酸盐价格数据"表）
- **关键逻辑**：
  - 筛选 `spec == '磷矿石30%品位'` 的数据
  - 转换格式：`price`（元/吨）→ `今日均价（万元/吨）`（万元/吨）
  - 更新 `lfp_all_data.json` 中 `table_name == '磷酸盐价格数据'` 的表

### 15.7 常见错误与修复

| 错误现象 | 原因 | 解决方案 |
|---------|------|---------|
| LFP产业链全景图磷矿石卡片显示"暂无数据" | `lfp_all_data.json` 中 `磷酸盐价格数据` 表数据为空 | 运行 `fix_lfp_all_data_phosphate.py` 修复 |
| LFP产业链全景图磷矿石卡片月环比计算错误 | `index_v3.html` 表名匹配错误（`磷酸盐价格` → 应为 `磷酸盐价格数据`） | 检查第12329行，`table_name === '磷酸盐价格数据'` |
| 有色板块甘特图点击磷矿石后显示"未找到对应的图表配置" | `svgMap` 中缺少磷矿石映射 | 检查 `renderSectorGantt()` 中的 `svgMap`，添加 `{ keyword: '磷矿石', svgId: 'phosphateChart' }` |
| 有色板块甘特图磷矿石行不显示最新数据 | `market_cumulative_metals.json` 未更新 | 运行 `add_phosphate_to_metals.py` 重新生成 |
| 磷矿石折线图不显示或显示异常 | `renderPhosphateChart()` 未被调用 | 检查 `renderSectorGantt()` 中的 `renderMap`，确保 `'phosphateChart': 'renderPhosphateChart'` |

### 15.8 验证方法

更新后，在浏览器访问 `http://localhost:8888/index_v3.html`，检查：

1. **有色板块甘特图**：
   - 滚动到"有色板块2026年累计涨跌幅"甘特图
   - 确认"磷矿石"行存在，显示累计涨跌幅（如 `+3.00%`）
   - 点击"磷矿石"行，下方展开折线图
   - 折线图显示2026年1月至今的价格走势

2. **LFP产业链全景图**：
   - 滚动到"磷酸铁锂产业链全景图"
   - 确认"磷矿石"卡片存在，显示最新价和月环比
   - 价格应为元/吨单位

3. **控制台无错误**：
   - 打开浏览器开发者工具（F12）
   - 切换"控制台"标签
   - 确认无红色错误信息

---

## 十六、首页（index_v3.html）数据源和更新流程

> **⚠️ 元数据（每次更新时必须同步修改！）**
> - **最后更新**：2026-06-08
> - **版本**：v1.0
> - **维护人**：AI Assistant
> - **适用范围**：`index_v3.html` 首页所有数据源和更新流程

### 16.1 概述

首页（`index_v3.html`）是信息中台的主入口，包含多个数据展示模块。本章节详细说明首页所有数据源的位置、更新方式和验证方法。

**首页主要数据模块**：

| 模块 | 数据文件 | 更新方式 | 说明 |
|------|---------|----------|------|
| 市场监控卡片 | `reports/market_lc.json` | 自动化任务 | 碳酸锂期货市场数据 |
| 锂电板块Dashboard | `reports/lfp_power_history.json` | 自动化任务 | LFP动力历史价格 |
| 锂电板块Dashboard | `reports/lfp_storage_history.json` | 自动化任务 | LFP储能历史价格 |
| 锂电板块Dashboard | `reports/lc_futures_history.json` | 自动化任务 | 碳酸锂期货历史 |
| 锂电板块Dashboard | `reports/iron_phosphate_history.json` | 自动化任务 | 磷酸铁历史价格 |
| 锂电板块Dashboard | `reports/market_cumulative_metals.json` | 脚本生成 | 有色板块累计涨跌幅 |
| 锂电板块Dashboard | `reports/market_cumulative.json` | 脚本生成 | 市场累计涨跌幅 |
| 锂电板块Dashboard | `reports/lfp_all_data.json` | 脚本生成 | LFP全产业链数据 |
| 化工板块甘特图 | `reports/wti_history.json` | 自动化任务 | WTI原油历史价格 |
| 化工板块甘特图 | `reports/ur_history.json` | 自动化任务 | 尿素期货历史价格 |
| 化工板块甘特图 | `reports/pt_futures_history.json` | 自动化任务 | 铂期货历史价格 |
| 化工板块甘特图 | `reports/pd_futures_history.json` | 自动化任务 | 钯期货历史价格 |
| 化工板块甘特图 | `reports/eg_futures_history.json` | 自动化任务 | 乙二醇期货历史价格 |
| 碳酸锂现货 | `carbonate_spot_price_merged.json` | 脚本生成 | 碳酸锂现货价格合并数据 |
| 热点新闻 | `hot_news_data.json` | 自动化任务 | 热点新闻数据 |
| BU Logo | `bu_logos.json` | 手动编辑 | BU Logo数据 |
| 产业链配置 | `data/industry_chain.json` | 手动编辑 | 产业链配置数据 |
| 锂辉石价格 | `data/lithium_ore_price_history.json` | 自动化任务 | 锂辉石价格历史 |
| 锂云母价格 | `data/lepidolite_price_history.json` | 自动化任务 | 锂云母价格历史 |
| 磷矿石价格 | `data/phosphate_rock_price.json` | 手动/脚本 | 磷矿石现货价格 |

### 16.2 市场监控数据（market_lc.json）

#### 16.2.1 数据文件结构

**`reports/market_lc.json`**（碳酸锂期货市场数据）：

```json
{
  "update_time": "2026-06-05 16:58:30",
  "is_trading_day": true,
  "trading_note": "",
  "source": "广期所",
  "contracts": [
    {
      "code": "LC0",
      "name": "碳酸锂主力连续",
      "price": 158820.0,
      "change_pct": 2.62,
      "change_amt": 4060.0,
      "high": 159940.0,
      "low": 152320.0,
      "open": 152320.0,
      "prev_close": 154760.0,
      "secid": "113.LC0",
      "market_id": "113",
      "trend": "up",
      "detail": {
        "volume": 333,
        "turn_over": 0,
        "open_interest": 0,
        "amplitude": 0
      }
    }
  ],
  "futures": []
}
```

**关键字段说明**：
- `update_time`：数据更新时间
- `is_trading_day`：是否交易日
- `contracts`：合约列表（LC0主力连续）
- `price`：最新价（元/吨）
- `change_pct`：涨跌幅（%）

#### 16.2.2 更新方式

**`market_lc.json`（碳酸锂期货市场数据）**：
- **自动化任务**：每日 17:00 自动执行
- **生成脚本**：`_archive/debug/update_all_futures_daily.py`
- **数据来源**：AkShare（广期所API）
- **输出文件**：`reports/market_lc.json`, `reports/lc_futures_history.json`, `reports/wti_history.json`, `reports/ur_history.json`
- **手动更新**：
```bash
cd "D:/trae/AI Daily report/_archive/debug"
python update_all_futures_daily.py
```

#### 16.2.3 验证方法

更新后，在浏览器访问 `http://localhost:8888/index_v3.html`，检查：
1. 市场监控卡片显示最新碳酸锂价格
2. 价格、涨跌幅数据正确
3. 控制台无错误

### 16.3 锂电板块Dashboard数据

#### 16.3.1 LFP动力/储能历史价格（lfp_power_history.json, lfp_storage_history.json）

**数据文件结构**：

```json
{
  "symbol": "磷酸铁锂(动力型)",
  "latest": {
    "date": "2026-06-05",
    "close": 63850,
    "change": 2800,
    "change_pct": 4.59
  },
  "history": [
    {"date": "2026-01-06", "open": 43950, "high": 46500, "low": 43950, "close": 44950}
  ]
}
```

**更新方式**：
- **自动化任务**：每日 12:00 自动执行（任务ID: `automation-1780278285589`，任务名称：磷酸铁锂+磷酸铁现货价格自动抓取（合并））
- **生成脚本**：`fetch_lfp_history.py`
- **数据来源**：我的钢铁网
- **输出文件**：`reports/lfp_power_history.json`, `reports/lfp_storage_history.json`
- **手动更新**：
```bash
cd "D:/buddy"
py -3.12 fetch_lfp_history.py
```

#### 16.3.2 碳酸锂期货历史（lc_futures_history.json）

**数据文件结构**：

```json
{
  "symbol": "碳酸锂主力连续",
  "latest": {
    "date": "2026-06-05",
    "close": 158820,
    "change_pct": 2.62
  },
  "history": [
    {"date": "2026-01-06", "open": 152320, "high": 159940, "low": 152320, "close": 158820}
  ]
}
```

**更新方式**：
- **自动化任务**：每日 17:00 自动执行（任务ID: `1780225924984`）
- **生成脚本**：`_archive/debug/update_all_futures_daily.py`
- **数据来源**：AkShare（广期所API）
- **输出文件**：`reports/lc_futures_history.json`, `reports/market_lc.json`, `reports/wti_history.json`, `reports/ur_history.json`
- **手动更新**：
```bash
cd "D:/trae/AI Daily report/_archive/debug"
python update_all_futures_daily.py
```

#### 16.3.3 磷酸铁历史价格（iron_phosphate_history.json）

**数据文件结构**：

```json
{
  "symbol": "磷酸铁",
  "latest": {
    "date": "2026-06-05",
    "close": 6850,
    "change_pct": -1.15
  },
  "history": [...]
}
```

**更新方式**：
- **自动化任务**：每日 12:00 自动执行（任务ID: `automation-1780278285589`，任务名称：磷酸铁锂+磷酸铁现货价格自动抓取（合并））
- **生成脚本**：`fetch_iron_phosphate_history.py`
- **数据来源**：我的钢铁网
- **输出文件**：`reports/iron_phosphate_history.json`
- **手动更新**：
```bash
cd "D:/buddy"
py -3.12 fetch_iron_phosphate_history.py
```

#### 16.3.4 有色板块累计涨跌幅（market_cumulative_metals.json）

**数据文件结构**：见第十五章 15.1 节

**更新方式**：
- **脚本生成**：基于 `data/phosphate_rock_price.json` 等数据计算
- **生成脚本**：`add_phosphate_to_metals.py`（详见第十五章）
- **输出文件**：`reports/market_cumulative_metals.json`

#### 16.3.5 市场累计涨跌幅（market_cumulative.json）

**数据文件结构**：

```json
{
  "meta": {
    "update_time": "2026-06-08 13:00:00",
    "data_source": "东方财富"
  },
  "products": [
    {
      "name": "碳酸锂",
      "start_date": "2026-01-04",
      "end_date": "2026-06-08",
      "start_price": 152000,
      "end_price": 158820,
      "change_pct": 4.49
    }
  ]
}
```

**更新方式**：
- **脚本生成**：`_archive/debug/generate_market_cumulative.py`
- **数据来源**：东方财富API（AkShare）
- **输出文件**：`reports/market_cumulative.json`
- **手动更新**：
```bash
cd "D:/trae/AI Daily report/_archive/debug"
python generate_market_cumulative.py
```

#### 16.3.6 LFP全产业链数据（lfp_all_data.json）

**数据文件结构**：见第十五章 15.1 节

**文件位置**：
- `D:\trae\AI Daily report\lfp_all_data.json`（主文件）
- `D:\trae\AI Daily report\data\lfp_all_data.json`
- `D:\trae\AI Daily report\reports\lfp_all_data.json`

**更新方式**：
- **关联脚本**：`fix_lfp_all_data_phosphate.py`（位于 `D:\buddy\` 根目录）
  - 用途：专门修复 lfp_all_data.json 中磷矿石数据为空的问题，将 phosphate_rock_price.json 中的磷矿石30%品位数据转换并注入到 lfp_all_data.json 的"磷酸盐价格数据"表中
- **配套脚本**：`inject_lfp_apr2026.py` — 将 AI 生成的文字叙事注入到 lfp_report_2026_04.html 报告，读取 lfp_all_data.json 作为数据源之一
- **⚠️ 可能也由自动化任务生成**：`automation-1780303846209`（数据库Excel→嵌入数据JS自动生成）可能也生成此文件
- **git 状态**：2026-06-08 显示 `M reports/lfp_all_data.json`（已修改未提交）
- **输出文件**：`lfp_all_data.json`（多个位置）

### 16.4 化工板块数据（甘特图）

#### 16.4.1 WTI原油历史（wti_history.json）

**数据文件结构**：

```json
{
  "symbol": "WTI原油",
  "latest": {
    "date": "2026-06-05",
    "close": 72.45,
    "change_pct": 1.23
  },
  "history": [...]
}
```

**更新方式**：
- **自动化任务**：每日 17:00 自动执行（任务ID: `1780225924984`）
- **生成脚本**：`_archive/debug/update_all_futures_daily.py`
- **数据来源**：AkShare（东方财富/新浪期货数据）
- **输出文件**：`reports/wti_history.json`, `reports/ur_history.json`, `reports/lc_futures_history.json`, `reports/market_lc.json`
- **手动更新**：
```bash
cd "D:/trae/AI Daily report/_archive/debug"
python update_all_futures_daily.py
```

#### 16.4.2 尿素期货历史（ur_history.json）

**更新方式**：
- **自动化任务**：每日 17:00 自动执行（任务ID: `1780225924984`）
- **生成脚本**：`_archive/debug/update_all_futures_daily.py`（或 `_update_wti_ur_data.py`）
- **数据来源**：AkShare（东方财富/新浪期货数据）
- **输出文件**：`reports/ur_history.json`

#### 16.4.3 铂/钯期货历史（pt_futures_history.json, pd_futures_history.json）

**更新方式**：
- **自动化任务**：每日 17:00 自动执行（任务ID: `1780225924984`，任务名称：期货品种每日收盘数据统一更新（含校验+推送））
- **生成脚本**：`_archive/debug/update_all_futures_daily.py`
- **数据来源**：AkShare（东方财富/新浪期货数据）
- **输出文件**：`reports/pt_futures_history.json`, `reports/pd_futures_history.json`

#### 16.4.4 乙二醇期货历史（eg_futures_history.json）和铁矿石期货（i_futures_history.json）

**更新方式**：
- **自动化任务**：每日 17:00 自动执行（任务ID: `1780225924984`，任务名称：期货品种每日收盘数据统一更新（含校验+推送））
- **生成脚本**：`_archive/debug/update_all_futures_daily.py`
- **数据来源**：AkShare（东方财富/新浪期货数据）
- **输出文件**：`reports/eg_futures_history.json`, `reports/i_futures_history.json`

### 16.5 碳酸锂现货数据（carbonate_spot_price_merged.json）

**数据文件结构**：

```json
{
  "update_time": "2026-06-05 16:58:30",
  "battery_grade": {
    "latest_price": 152000,
    "history": [...]
  },
  "industrial_grade": {
    "latest_price": 148000,
    "history": [...]
  }
}
```

**更新方式**：
- **自动化任务**：每日自动执行（任务ID: `automation-1779169063282`，任务名称：碳酸锂+磷矿石现货价格自动抓取）
- **生成脚本**：`fetch_carbonate_spot_auto.py`
- **数据来源**：金投价格网（https://jiage.cngold.org/）
- **输出文件**：`carbonate_spot_price_merged.json`（根目录和 data/ 目录）
- **手动更新**：
```bash
cd D:/buddy
py -3.12 fetch_carbonate_spot_auto.py
```
- **数据内容**：电池级（battery_grade）、工业级（industrial_grade）碳酸锂现货价格历史
- **输出文件**：`carbonate_spot_price_merged.json`（根目录和 data/ 目录）

### 16.6 热点新闻数据（hot_news_data.json）

**数据文件结构**：

```json
{
  "update_time": "2026-06-08 09:30:00",
  "news": [
    {
      "title": "新闻标题",
      "summary": "新闻摘要",
      "source": "来源",
      "date": "2026-06-08",
      "priority": "P0"
    }
  ]
}
```

**更新方式**：
- **自动化任务**：工作日自动生成（任务：`hot-news-generator`）
- **生成脚本**：`generate_hot_news.py`（根目录）
- **数据来源**：从 `reports/YYYY-MM-DD-XXX.json` 提取"今日关注"新闻
- **输出文件**：`hot_news_data.json`
- **手动更新**：
```bash
cd "D:/trae/AI Daily report"
python generate_hot_news.py
```

### 16.7 其他数据文件

#### 16.7.1 BU Logo数据（bu_logos.json）

**数据文件结构**：

```json
{
  "lhy": "data:image/png;base64,...",
  "czly": "data:image/png;base64,..."
}
```

**更新方式**：
- **手动编辑**：直接编辑 `bu_logos.json` 文件
- **说明**：Base64编码的Logo图片数据

#### 16.7.2 产业链配置数据（data/industry_chain.json）

**数据文件结构**：

```json
{
  "锂电板块": {
    "description": "磷酸铁锂产业链从上游原料到终端应用",
    "nodes": [...],
    "edges": [...]
  }
}
```

**更新方式**：
- **手动编辑**：直接编辑 `data/industry_chain.json` 文件
- **说明**：产业链节点和边的关系配置

#### 16.7.3 锂辉石/锂云母价格历史（lithium_ore_price_history.json, lepidolite_price_history.json）

**更新方式**：
- **自动化任务**：每日自动执行（任务ID: `1779788037620`，任务名称：锂辉石锂云母价格数据自动更新）
- **生成脚本**：`fetch_lithium_ore_price.py`（抓取）、`update_lithium_lepidolite_history.py`（更新历史）
- **数据来源**：我的钢铁网
- **输出文件**：`data/lithium_ore_price_history.json`, `data/lepidolite_price_history.json`
- **详细说明**：见第十四章

#### 16.7.4 磷矿石价格（phosphate_rock_price.json）

**更新方式**：
- **手动/脚本更新**：详见第十五章
- **输出文件**：`data/phosphate_rock_price.json`

### 16.8 品种行情条（ticker-bar）

#### 16.8.1 功能说明

首页导航栏下方展示9个品种的最新价格和涨跌幅，点击可跳转到对应的市场行情监控图表。

#### 16.8.2 数据源

| 品种 | ticker ID | 数据源文件 | 更新方式 | 说明 |
|------|-----------|-----------|----------|------|
| 碳酸锂期货 | `ticker-lc-futures` | `reports/lc_futures_history.json` | 自动化任务 (1780225924984) | 广期所LC2609主力合约 |
| 电池级碳酸锂 | `ticker-lc-spot` | `carbonate_spot_price_merged.json` (battery_grade) | 自动化任务 (1779169063282) | 金投价格网 |
| WTI原油 | `ticker-wti` | `reports/wti_history.json` | 自动化任务 (1780225924984) | 国际原油期货 |
| 磷酸铁锂 | `ticker-lfp` | `reports/lfp_power_history.json` | 自动化任务 (1780278285589) | 动力型价格 |
| 锂辉石 | `ticker-spod` | `data/lithium_ore_price_history.json` | 自动化任务 (1779788037620) | 澳洲CIF 5% |
| 锂云母 | `ticker-lep` | `data/lepidolite_price_history.json` | 自动化任务 (1779788037620) | 江西2.5% |
| 尿素 | `ticker-urea` | `reports/ur_history.json` | 自动化任务 (1780225924984) | 郑商所UR2609 |
| 铁矿石 | `ticker-iron` | `reports/i_futures_history.json` | 自动化任务 (1780225924984) | 大商所I0主力 |
| 乙二醇 | `ticker-eg` | `reports/eg_futures_history.json` | 自动化任务 (1780225924984) | 大商所EG0主力 |

#### 16.8.3 HTML结构

品种行情条位于 `index_v3.html` 第9041-9089行。每个 `ticker-item` 的 `onclick` 属性指向对应图表的锚点ID。

#### 16.8.4 锚点设置

为使品种卡片点击后跳转到对应图表，需在市场行情监控区域为每个图表设置锚点ID：

| 品种 | 图表锚点ID | 锚点位置（index_v3.html） |
|------|------------|---------------------------|
| 碳酸锂期货 | `sec-lc-futures` | 碳酸锂期货图表卡片 |
| 电池级碳酸锂 | `sec-lc-spot` | 电池级碳酸锂图表卡片 |
| WTI原油 | `sec-wti` | WTI原油图表卡片 |
| 磷酸铁锂 | `sec-lfp` | 磷酸铁锂双图卡片 |
| 锂辉石 | `sec-spod` | 锂辉石图表卡片 |
| 锂云母 | `sec-lep` | 锂云母图表卡片 |
| 尿素 | `sec-urea` | 尿素期货图表卡片 |
| 铁矿石 | `sec-iron` | 铁矿石期货图表卡片 |
| 乙二醇 | `sec-eg` | 乙二醇期货图表卡片 |

#### 16.8.5 更新方式

**手动更新**：如需修改品种名称、顺序或链接，直接编辑 `index_v3.html` 第9041-9089行的 `ticker-bar` 部分。

**自动化更新**：各品种价格数据由对应自动化任务自动更新，无需手动干预。

#### 16.8.6 验证方法

更新后验证：
1. 打开首页 `http://localhost:8888/index_v3.html`
2. 检查9个品种卡片是否显示最新价格
3. 点击每个品种卡片，验证是否跳转到对应图表
4. F12控制台无错误

### 16.9 常见问题与修复

| 错误现象 | 原因 | 解决方案 |
|---------|------|---------|
| 市场监控卡片显示旧数据 | `market_lc.json` 未更新 | 检查自动化任务是否运行，或手动更新 |
| LFP动力/储能图表不显示 | `lfp_power_history.json` 或 `lfp_storage_history.json` 数据格式错误 | 检查数据文件格式，重新生成 |
| 化工板块甘特图不显示 | `wti_history.json` 等文件不存在或格式错误 | 检查文件是否存在，重新生成 |
| 碳酸锂现货图表不显示 | `carbonate_spot_price_merged.json` 数据格式错误 | 检查数据格式，重新生成 |
| 热点新闻不显示 | `hot_news_data.json` 不存在或格式错误 | 检查文件是否存在，重新生成 |

### 16.10 验证清单

更新首页数据后，按以下清单验证：

1. **市场监控卡片**：
   - [ ] 碳酸锂价格显示正确
   - [ ] 磷酸铁锂价格显示正确
   - [ ] 涨跌幅计算正确

2. **锂电板块Dashboard**：
   - [ ] LFP动力/储能图表显示正确
   - [ ] 碳酸锂期货图表显示正确
   - [ ] 磷酸铁价格图表显示正确
   - [ ] 有色板块甘特图显示正确

3. **化工板块甘特图**：
   - [ ] WTI原油图表显示正确
   - [ ] 尿素期货图表显示正确
   - [ ] 铂/钯期货图表显示正确
   - [ ] 乙二醇期货图表显示正确

4. **碳酸锂现货**：
   - [ ] 电池级图表显示正确
   - [ ] 工业级图表显示正确

5. **热点新闻**：
   - [ ] 新闻轮播显示正确
   - [ ] 新闻内容完整

6. **控制台**：
   - [ ] 打开F12开发者工具
   - [ ] 控制台无红色错误信息

---

## 十七、电解液数据更新流程

> **⚠️ 元数据（每次更新时必须同步修改！）**
> - **最后更新**：2026-06-11
> - **版本**：v1.0
> - **维护人**：AI Assistant
> - **适用范围**：`electrolyte_data.js`，首页电解液Tab展示

### 17.1 概述

电解液数据是首页"锂电板块行情 → 电解液"Tab的数据源，包含7个品种的日报价数据。

**7个品种**：

| # | 品种ID | 品种名称 | 数据来源 |
|---|-------|---------|---------|
| 1 | 电解液价格-磷酸铁锂动力型 | 电解液-磷酸铁锂动力用 | SMM自动抓取 |
| 2 | 电解液价格-磷酸铁锂储能型 | 电解液-磷酸铁锂储能用 | SMM自动抓取 |
| 3 | 电解液价格-三元动力型 | 电解液-三元动力用 | SMM自动抓取 |
| 4 | LiFSI价格-固态 | 双氟磺酰亚胺锂-LiFSI | SMM自动抓取 |
| 5 | 添加剂VC-价格 | 碳酸亚乙烯酯-电池级 | SMM自动抓取 |
| 6 | 添加剂PS-价格 | 苯基磺酸-电池级 | SMM自动抓取 |
| 7 | 添加剂FEC-价格 | 氟代碳酸乙烯酯-电池级 | SMM自动抓取 |

### 17.2 数据文件

| 文件 | 说明 | 更新方式 |
|------|------|---------|
| `electrolyte_data.js` | 电解液7个品种的价格数据 | **自动化任务** |
| `smm_crawler.py` | SMM网站爬虫脚本 | 手动 |
| `electrolyte_smm_auto_update.py` | 自动更新脚本（调用爬虫+更新数据）| **自动化任务** |

### 17.3 数据源

| 项目 | 内容 |
|------|------|
| **网站** | SMM上海有色金属网 |
| **URL** | `https://newenergy.smm.cn/price/14042-15014` |
| **数据** | 电解液、LiFSI、添加剂、溶剂的日报价 |
| **特点** | 只提供当天数据，不提供历史数据 |

### 17.4 更新流程

#### 17.4.1 全自动化（SMM爬虫）

```
执行命令: python electrolyte_smm_auto_update.py

步骤：
1. 运行 smm_crawler.py → 自动访问SMM网站
2. 解析网页表格数据 → 保存到 smm_prices_YYYYMMDD_HHMMSS.json
3. 加载最新SMM数据 → 匹配品种到electrolyte_data.js
4. 追加当天数据 → 保存回 electrolyte_data.js
```

#### 17.4.2 手动更新（历史数据补充）

当需要补充历史数据时（因为SMM只提供当天数据）：

```
步骤：
1. 从SMM网站下载历史Excel数据（手动）
2. 运行: python electrolyte_auto_update.py [Excel文件路径]
3. 脚本自动匹配品种并更新数据
```

### 17.5 自动化任务配置

| 项目 | 内容 |
|------|------|
| **任务ID** | `automation-1781162527681` |
| **任务名称** | 电解液数据自动更新（全自动SMM爬虫）|
| **执行频率** | 每周一~周五 16:00 |
| **运行脚本** | `electrolyte_smm_auto_update.py` |
| **工作目录** | `D:\trae\AI Daily report` |

### 17.6 页面展示

首页"锂电板块行情" → 点击"电解液"Tab → 点击任意品种 → 展示折线图

**数据字段**：
- `日期`：日期（YYYY-MM-DD）
- `最低价`：最低价格（元/吨）
- `最高价`：最高价格（元/吨）
- `均价`：平均价格（元/吨）

### 17.7 验证方法

1. **访问首页**：`http://localhost:8888/index_v3.html`
2. **点击锂电板块行情** → **电解液Tab**
3. **��击任意品种**（如"磷酸铁锂动力"）
4. **验证**：
   - [ ] 折线图显示正常
   - [ ] 最新日期为2026-06-10（或更晚）
   - [ ] 最新价和涨跌幅显示正确
   - [ ] 控制台无红色错误

### 17.8 常见问题

| 问题 | 原因 | 解决方案 |
|------|------|---------|
| 图表不显示 | 数据文件加载失败 | 检查electrolyte_data.js是否完整 |
| 最新日期不正确 | 自动化任务未运行 | 手动运行 `python electrolyte_smm_auto_update.py` |
| 数据为0或异常 | SMM网站结构变化 | 检查smm_crawler.py是否需要更新 |

---

## 十九、子页面运维说明

> **⚠️ 元数据（每次更新时必须同步修改！）**
> - **最后更新**：2026-06-08
> - **版本**：v1.2
> - **维护人**：AI Assistant
> - **适用范围**：所有子页面（archive_v3.html, bu_hub.html, database_hub.html, strategy_hub.html, radar_hub.html, analysis_hub.html, dept-archive.html）

### 18.1 概述

除了首页（`index_v3.html`）外，系统还包含多个子页面，每个子页面有不同的数据源和更新流程。本章节详细说明各子页面的运维方法。

**子页面列表**：

| 页面文件 | 页面名称 | 数据源 | 更新方式 |
|---------|---------|---------|----------|
| `archive_v3.html` | 归档页面 | `reports/YYYY-MM-DD.json` | 随早报更新 |
| `bu_hub.html` | BU中心 | `reports/YYYY-MM-DD.json` | 随早报更新 |
| `database_hub.html` | 数据库中心 | 各数据库JS文件 | 手动更新 |
| `strategy_hub.html` | 战略中心 | `BU_STRATEGY_DATA` JS对象（内嵌） | 手动修改HTML |
| `radar_hub.html` | 雷达中心 | `reports/YYYY-MM-DD.json` | 随早报更新 |
| `analysis_hub.html` | 分析中心 | 纯静态（DATABASES数组） | 手动修改HTML |
| `dept-archive.html` | 部门归档 | `reports/YYYY-MM-DD.json` | 随早报更新 |

### 17.2 归档页面（archive_v3.html）

#### 17.2.1 功能说明

归档页面用于查看历史早报数据，支持按日期、事业部筛选。

#### 17.2.2 数据源

- **主数据**：`reports/YYYY-MM-DD.json`（早报数据）
- **索引文件**：`reports/index.json`（可用日期列表）

#### 17.2.3 更新方式

归档页面数据随早报系统自动更新，无需手动操作。

#### 17.2.4 验证方法

1. 访问 `http://localhost:8888/archive_v3.html`
2. 确认日期列表显示正确
3. 点击某个日期，确认早报内容显示正确

### 17.3 BU中心（bu_hub.html）

#### 17.3.1 功能说明

BU中心展示各事业部的详细数据和雷达图。

#### 17.3.2 数据源

- **主数据**：`reports/YYYY-MM-DD.json`（早报数据）
- **雷达图数据**：从 `reports/YYYY-MM-DD.json` 的 `departments[buId].dims` 字段加载；若当天早报数据不存在，则使用HTML中硬编码的静态默认数据（`BUS_DATA`）

#### 17.3.3 更新方式

随早报系统自动更新。

### 17.4 数据库中心（database_hub.html）

#### 17.4.1 功能说明

数据库中心展示7个数据库的数据，支持按数据库、日期筛选。

#### 17.4.2 数据源

- **数据库JS文件**：各数据库的embedded JS文件（详见第二章）

#### 17.4.3 更新方式

手动更新各数据库的embedded JS文件（详见第四章）。

### 17.5 战略中心（strategy_hub.html）

#### 17.5.1 功能说明

战略中心展示各BU竞对Top10排名、12个月趋势数据和行业汇总统计。

#### 17.5.2 数据源

- **数据源类型**：直接内嵌为 `BU_STRATEGY_DATA` JavaScript 对象（第148行开始）
- **数据结构**：
  - `top10` — 各BU竞对Top10排名（产量、销量、利用率）
  - `trend` — 12个月趋势数据（产量 + 开工率）
  - `stat` — 行业汇总统计（产量/开工率/出口/价格）
- **特点**：完全静态，无 `fetch` 调用
- **嵌入版**：`D:\trae\AI Daily report\embedded\strategy_hub.html` 和 `strategy_hub_embedded.html` 均存在

#### 17.5.3 更新方式

- **手动更新**：每次更新需手动修改 `strategy_hub.html` 文件中的 `BU_STRATEGY_DATA` JS 对象
- **更新步骤**：
  1. 打开 `strategy_hub.html` 文件
  2. 找到第148行开始的 `BU_STRATEGY_DATA` 对象
  3. 修改 `top10`、`trend`、`stat` 数据
  4. 保存文件
- **验证方法**：在浏览器访问 `http://localhost:8888/strategy_hub.html`，检查数据是否正确显示

### 17.6 雷达中心（radar_hub.html）

#### 17.6.1 功能说明

雷达中心展示各事业部的六维雷达图。

#### 17.6.2 数据源

- **主数据**：`reports/YYYY-MM-DD.json`（早报数据）
- **雷达图数据**：从 `reports/YYYY-MM-DD.json` 的 `departments[buId].dims` 字段加载

#### 17.6.3 更新方式

随早报系统自动更新。

### 17.7 分析中心（analysis_hub.html）

#### 17.7.1 功能说明

分析中心是纯静态导航页，展示各数据库报告的入口链接。

#### 17.7.2 数据源

- **数据源类型**：纯静态导航页，无数据加载
- **数据库配置**：在页面 JS 中定义（第265行起的 `DATABASES` 数组）
- **链接方式**：各数据库卡片通过 `reportHref` 链接到对应的 `.html` 报告文件
- **特点**：只改链接指向，不需要更新数据
- **嵌入版**：`D:\trae\AI Daily report\embedded\analysis_hub.html` 存在

#### 17.7.3 更新方式

- **手动更新**：如需修改数据库链接，直接编辑 `analysis_hub.html` 文件中的 `DATABASES` 数组
- **更新步骤**：
  1. 打开 `analysis_hub.html` 文件
  2. 找到第265行开始的 `DATABASES` 数组
  3. 修改对应数据库的 `reportHref` 字段
  4. 保存文件
- **验证方法**：在浏览器访问 `http://localhost:8888/analysis_hub.html`，检查数据库卡片链接是否正确

### 17.8 部门归档（dept-archive.html）

#### 17.8.1 功能说明

部门归档页面按部门展示历史早报。

#### 17.8.2 数据源

- **主数据**：`reports/YYYY-MM-DD.json`（早报数据）

#### 17.8.3 更新方式

随早报系统自动更新。

### 17.9 常见问题与修复

| 错误现象 | 原因 | 解决方案 |
|---------|------|---------|
| 归档页面显示旧数据 | `reports/index.json` 未更新 | 重新生成 `reports/index.json` |
| BU中心雷达图不显示 | 雷达图数据缺失 | 检查 `RADAR_HISTORY` 配置 |
| 数据库中心显示错误 | 数据库JS文件版本过旧 | 更新数据库JS文件 |
| 战略中心数据不显示 | `BU_STRATEGY_DATA` 数据过时 | 手动修改 `strategy_hub.html` 中的 `BU_STRATEGY_DATA` 对象 |
| 分析中心链接错误 | `DATABASES` 数组配置错误 | 手动修改 `analysis_hub.html` 中的 `DATABASES` 数组 |

---

## 二十、运维文档更新记录

> **本章记录运维文档本身的更新历史。**

| 日期 | 版本 | 更新内容 | 更新人 |
|------|------|---------|---------|
| 2026-06-11 | v1.8 | 添加第十七章（电解液数据更新流程）：7个品种、SMM数据源、自动化任务配置、手动/自动更新流程、验证方法、常见问题 | AI Assistant |
| 2026-06-09 | v1.7 | 更新第十七章 17.3.2（BU中心雷达图数据源）和 17.6.2（雷达中心雷达图数据源）：确认为来自 reports/YYYY-MM-DD.json 的 departments[buId].dims 字段 | AI Assistant |
| 2026-06-08 | v1.6 | 更新第十六章 16.8.2（品种行情条数据源）：WTI/尿素→automation-1780225924984，锂辉石/锂云母→automation-1779788037620；更新 16.7.3（锂辉石/锂云母）自动化任务信息 | AI Assistant |
| 2026-06-08 | v1.5 | 添加第十六章 16.8（品种行情条/ticker-bar）；更新 index_v3.html 添加图表锚点ID和ticker链接 | AI Assistant |
| 2026-06-08 | v1.4 | 更新第十六章 16.3.1/16.3.3/16.5 自动化任务信息（任务ID: automation-1780278285589, automation-1779169063282）；更新第十五章 15.4 磷矿石自动化任务信息 | AI Assistant |
| 2026-06-08 | v1.3 | 删除第十六章 market_lfp.json 引用；更新 pt/pd/eg/i_futures_history.json 自动化任务信息（任务ID: 1780225924984）；更新第十七章 17.5（strategy_hub.html）和 17.7（analysis_hub.html） | AI Assistant |
| 2026-06-08 | v1.2 | 添加第十六章（首页数据源和更新流程）、第十七章（子页面运维说明） | AI Assistant |
| 2026-05-31 | v1.1 | 添加第十四章（锂辉石/锂云母）、第十五章（磷矿石） | AI Assistant |
| 2026-05-20 | v1.0 | 初始版本，包含第一至十三章 | AI Assistant |

---

**[文档结束]**


