# 📦 AI Daily Report - 存档操作指南

**创建日期**: 2026-06-26  
**适用场景**: 在任何对话框中需要存档时参考

---

## 🎯 快速存档命令

### **方法1：智能存档（推荐）**

```bash
cd "D:/trae/AI Daily report"
python smart_git_commit.py
```

**功能**：
- ✅ 自动检测文件变化（SHA256哈希值）
- ✅ 只提交有变化的文件
- ✅ 自动推送到GitHub
- ✅ 输出详细日志

**输出示例**：
```
📝 data/recycling_all_data.json: 内容有变化
📝 index_v3.html: 内容有变化
⏭️  automotive_data_v2.html: 内容无变化
✅ 已提交 2 个文件
```

---

### **方法2：传统存档（备用）**

```bash
cd "D:/trae/AI Daily report"
git add -A
git commit -m "存档: YYYY-MM-DD 描述"
git push origin main
```

**适用场景**：智能存档脚本不可用时的备用方案

---

### **方法3：批处理脚本（最简单）**

**文件位置**: `D:\trae\AI Daily report\智能存档.bat`

**使用方法**: 双击运行即可

**优点**：
- ✅ 无需打开AI对话框
- ✅ 无需输入命令
- ✅ 适合快速存档

---

## 📋 存档范围

### **会被存档的文件**

| 类别 | 文件类型 | 示例 |
|------|---------|------|
| **主站页面** | `*.html` | `index_v3.html`, `index.html` |
| **子页面** | `*_hub.html`, `*_data_v2.html`, `*_charts.html` | 共51个HTML文件 |
| **早报数据** | `reports/*.json` | `reports/2026-06-26.json` |
| **数据库文件** | `data/*.json` | `data/recycling_all_data.json` |
| **配置文件** | `*.py`, `*.json`, `*.md` | `smart_git_commit.py` |

### **不会被存档的文件**

| 类别 | 文件类型 | 原因 |
|------|---------|------|
| **临时文件** | `*_temp.json`, `*_bak.json` | 被 `.gitignore` 排除 |
| **缓存文件** | `__pycache__/`, `node_modules/` | 被 `.gitignore` 排除 |
| **WorkBuddy数据** | `.workbuddy/` | 被 `.gitignore` 排除 |

---

## 🔧 智能存档脚本详解

### **脚本位置**
`D:\trae\AI Daily report\smart_git_commit.py`

### **配置文件**
`D:\trae\AI Daily report\smart_commit_config.json`

### **配置内容**
```json
{
  "compare_rules": {
    "*.html": {"method": "hash", "quick_check": "size"},
    "data/*.json": {"method": "hash", "quick_check": "size"}
  },
  "exclude_patterns": [
    "data/*_temp.json",
    "data/*_bak.json"
  ],
  "debug_mode": false
}
```

### **自定义配置**

如果你希望：
- 只比较文件大小（更快）：修改 `"method": "size"`
- 排除更多文件：在 `"exclude_patterns"` 中添加规则
- 启用调试模式：修改 `"debug_mode": true`

---

## 🧪 验证存档是否成功

### **验证方法1：查看Git日志**

```bash
cd "D:/trae/AI Daily report"
git log --oneline -5
```

**应该看到**：
```
78809cb 智能存档: 2026-06-26 51个文件有变化
```

### **验证方法2：访问GitHub Pages**

打开：`https://bishop801818-debug.github.io/ai-daily-report/`

**应该能看到**：最新更新的页面和数据

### **验证方法3：检查Git状态**

```bash
cd "D:/trae/AI Daily report"
git status
```

**应该显示**：`nothing to commit, working tree clean`

---

## 🚨 常见问题

### **问题1：智能存档脚本报错**

**可能原因**：Python环境配置问题

**解决方案**：
```bash
# 使用完整Python路径
C:/Users/1/.workbuddy/binaries/python/versions/3.13.12/python.exe smart_git_commit.py
```

### **问题2：Git推送失败**

**可能原因**：远程仓库有更新

**解决方案**：
```bash
git pull --rebase
git push origin main
```

### **问题3：文件没有被存档**

**可能原因**：文件被 `.gitignore` 排除

**解决方案**：
```bash
# 检查文件是否被Git跟踪
git ls-files <文件名>

# 如果被排除，强制添加
git add -f <文件名>
```

---

## 📝 存档检查清单

在存档前，建议检查：

- [ ] 早报数据已更新（`reports/2026-06-26.json`）
- [ ] 首页版本号已更新（`index_v3.html`）
- [ ] 政策中心数据已更新（`reports/policies.json`）
- [ ] 所有HTML文件版本号已更新

在存档后，建议验证：

- [ ] Git日志显示最新提交
- [ ] GitHub Pages显示最新数据
- [ ] `git status` 显示无未提交文件

---

## 🎯 总结

**推荐使用顺序**：

1. **优先使用**: 双击 `智能存档.bat`（最简单）
2. **次选**: 运行 `python smart_git_commit.py`（智能存档）
3. **备用**: 传统 `git add -A` + `git commit` + `git push`（手动存档）

**有任何问题，参考本文档或联系开发者。**

---

**文档版本**: v1.0  
**最后更新**: 2026-06-26
