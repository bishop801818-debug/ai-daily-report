# 锂电池回收数据自动更新 - 定时任务设置说明

## 任务说明
- **任务名称**: 回收数据自动更新
- **执行时间**: 每个工作日 17:00
- **执行脚本**: `run_recycling_update.bat`
- **日志文件**: `recycling_update_log.txt`

## 设置步骤

### 方法一：使用批处理文件（推荐）

1. 打开 `D:\trae\AI Daily report\run_recycling_update.bat`
2. 右键点击 → "创建快捷方式"
3. 将快捷方式复制到 `%appdata%\Microsoft\Windows\Start Menu\Programs\Startup` (开机启动文件夹)
   - 或者：按 `Win+R`，输入 `shell:startup`，回车，粘贴快捷方式

**注意**: 此方法只能实现开机运行，无法实现定时运行。如需定时运行，请使用方法二。

### 方法二：使用Windows任务计划程序（推荐）

1. 按 `Win+R`，输入 `taskschd.msc`，回车，打开"任务计划程序"
2. 右侧点击 "创建任务"
3. 填写任务信息：
   - **名称**: 回收数据自动更新
   - **描述**: 每个工作日17:00自动从SMM网站抓取锂电池回收价格数据并更新到recycling_data_v2.js
   - **安全选项**: 勾选"不管用户是否登录都要运行"，勾选"使用最高权限"

4. 切换到"触发器"选项卡：
   - 点击"新建"
   - **开始任务**: 按预定计划
   - **设置**: 每周
   - **星期**: 勾选 星期一、星期二、星期三、星期四、星期五
   - **时间**: 17:00:00
   - **启用**: 勾选
   - 点击"确定"

5. 切换到"操作"选项卡：
   - 点击"新建"
   - **操作**: 启动程序
   - **程序或脚本**: `D:\trae\AI Daily report\run_recycling_update.bat`
   - **起始于**: `D:\trae\AI Daily report\`
   - 点击"确定"

6. 切换到"条件"选项卡：
   - 取消勾选"只有在计算机使用交流电源时才启动此任务"（如果是台式机）
   - 勾选"唤醒计算机运行此任务"

7. 切换到"设置"选项卡：
   - 勾选"如果任务失败，重新启动每隔": 10分钟，最多 3 次
   - 勾选"如果请求后任务还在运行，强行将其停止": 1小时
   - 勾选"如果任务未在计划时间运行，立即运行"

8. 点击"确定"保存任务

### 方法三：使用命令（需要管理员权限）

以管理员身份打开命令提示符（CMD），执行：

```cmd
schtasks /Create /TN "回收数据自动更新" /TR "D:\trae\AI Daily report\run_recycling_update.bat" /SC WEEKLY /D MON,TUE,WED,THU,FRI /ST 17:00 /F
```

## 测试任务

### 手动运行测试
1. 打开"任务计划程序"
2. 找到"回收数据自动更新"任务
3. 右键点击 → "运行"
4. 检查 `recycling_update_log.txt` 查看运行日志

### 检查下次运行时间
在"任务计划程序"中，查看"下次运行时间"列。

## 故障排查

### 问题1：任务没有运行
- 检查计算机是否开机
- 检查任务是否启用
- 检查"下次运行时间"是否正确

### 问题2：脚本运行失败
- 检查 `recycling_update_log.txt` 日志文件
- 手动运行 `run_recycling_update.bat` 查看错误信息
- 检查Python路径是否正确：`C:\Users\1\.workbuddy\binaries\python\versions\3.13.12\python.exe`

### 问题3：数据没有更新
- 检查SMM网站是否有当天数据（通常当天17:00后才有数据）
- 检查 `recycling_prices_*.json` 文件是否有当天数据
- 检查 `recycling_data_v2.js` 是否包含当天数据

## 文件清单
- `recycling_smm_auto_update_v2.py` - 主更新脚本
- `recycling_smm_crawler.py` - SMM爬虫脚本
- `run_recycling_update.bat` - 批处理文件（定时任务执行）
- `recycling_update_log.txt` - 运行日志
- `recycling_data_v2.js` - 数据文件（被更新）

## 紧急恢复

如果定时任务失败导致数据错误：
1. 打开Git Bash
2. 执行 `cd D:/trae/AI\ Daily\ report && git log --oneline -10` 查看历史提交
3. 执行 `git checkout <commit-hash> -- recycling_data_v2.js` 恢复数据文件
4. 执行 `git checkout HEAD -- recycling_data_v2.js` 取消恢复（如果恢复错了）

## 联系人
如有问题，请联系开发团队。
